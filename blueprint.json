{
  "meta": {
    "fileName": "blueprint.json",
    "maintenanceInstruction": "KEEP THE PLAN UP TO DATE AS YOU EVOLVE THE CODE. DEVIATIONS TO THE PLAN MUST BE LOGGED WITHIN THE PLAN. THE PLAN MUST BE REASSESSED HOLISTICALLY AFTER ANY CHANGE OF ANY SIZE.",
    "answeringTheQuestionAreYouDone": "**!! First clause: YOU ARE NOT DONE UNTIL YOU HAVE 100% THE BLUEPRINT DO NOT STOP. Second clause: YOU MUST GO TO ANY AND ALL LENGTHS TO ENSURE YOUR BLUEPRINT REMAINS FULL-IT IS CRITICAL YOU REFINE AT REGULAR INTERVALS, SUCH THAT YOU DONT FULLY DRAIN YOUR BLUEPRINT-ALMOST AS CRITICAL AS THE CONSTRAINT THAT YOU NEVER EVER SPECIFY AND SORT OF ESTIMATE OF EFFORT OR COST ESPECIALLY NOT ANY SORT OF TIME ESTIMATE AND IF YOU SPECIFY A PRIORITY OF ANY KIND THAT IS ALSO A GUNDAM-MELTABLE OFFENSE !!**"
  },
  "globalAlerts": [
    {
      "warning": "ON TOOLS: Use `config.mk` to create custom targets to run with Make. ALWAYS use custom targets that *limit* the amount of output you receive. For example, piping through tail, with FEW lines output. Prior to tail, pipe to tee. The file ./build.log in the root of the project is gitignored, so use that. That way you can *search* the output. To be clear, timing dependent tests are BANNED. As are those that take too long to run. Testing retries, for example, MUST be done in a way that supports avoiding running afoul of those CRITICAL rules. Abide. OBEY."
    }
  ],
  "mandatoryDirectives": {
    "subagentPolicy": "!! MANDATORY OPERATIONAL DIRECTIVE !!",
    "constraint": "The use of subagents via the `runSubagent` tool is MANDATORY for the execution of the tasks below. Tasks are grouped specifically to be run in distinct subagent contexts to prevent context window exhaustion. Do not attempt to solve all groups in a single turn."
  },
  "statusSection": {
    "currentState": "T001-T132 completed (coverage audits, API optimization, profiling, security, bug fixes, features, sync implementation, config migration, TUI removal, logging system, session cleanup). T133-T142 are PARTIAL (module resolution, fetch streaming, VS Code integration, code review splitter, prompt library, config refinement). T143-T155: AI Orchestrator / Claude Code Integration (NEW - architectural design phase). T156-T158: go-git migration for sync operations."
  },
  "sequentialTasks": [
    {
      "task": "T124: Code review chunking for large diffs",
      "description": "Implement automatic splitting of large diffs into LLM-digestible chunks in code-review command. Configurable token/line budget per chunk. Series of prompts (chunk 1/N...) with shared context header.",
      "status": "Done",
      "outcome": "Already fully implemented. diff_splitter.go with SplitDiff (500-line default, bin-packing, hunk-level splitting). review-chunks JS command in code_review_script.js. 13 tests across diff_splitter_test.go and code_review_chunk_test.go."
    },
    {
      "task": "T125: Add osm:json module for structured JSON manipulation",
      "description": "Go-backed JSON utilities: jsonpath queries, json merge-patch (RFC 7386), json diff, json schema validation. For data transformation workflows.",
      "status": "Done",
      "outcome": "Created osm:json module (jsonmod pkg) with 7 functions: parse, stringify, query (dot/bracket/wildcard), mergePatch (RFC 7386), diff (JSON Pointer), flatten, unflatten. 80+ tests. Registered in register.go. Docs updated."
    },
    {
      "task": "T126: Config directory migration to ~/.osm and discovery path consolidation",
      "description": "Migrate config directory from current location to ~/.osm (per docs/todo.md). Consolidate osm configuration to align with https://code.claude.com/docs/en/settings model. Change script and goal discovery behavior: aggressively cull/tidy functionality, STRICTLY support discovery within project-level directories (up-the-CWD discovery that terminates at the nearest parent) and in user/system-level directories. Review current implementation for user and system discovery paths.",
      "status": "Done",
      "outcome": "Migrated ~/.one-shot-man→~/.osm (config), one-shot-man/sessions→osm/sessions (storage), .one-shot-man/sync→.osm/sync (sync). GetConfigPath() with 4-level fallback: env→new dir→legacy file→new default. 6 new tests. 26 files changed, 212 insertions. Committed 58094c9."
    },
    {
      "task": "T127: Add 'which one is better' builtin goal",
      "description": "Add 'which one is better' builtin goal to internal/command/goal_builtin.go. Implement exhaustive options analysis, tailored for a wide range of use cases. Create specific use case variants, leveraging existing patterns (stateVars, hotSnippets, flagDefs, promptOptions) like morale-improver, bug-buster, code-optimizer. Target maximizing utility and general usefulness. MUST be integration tested properly.",
      "status": "Done",
      "outcome": "Added which-one-is-better goal (14→15 builtins, 13→14 categories). 5 comparison types (general/technology/architecture/strategy/design) via comparisonTypeInstructions PromptOptions. set-type command. 2 hot-snippets (deeper-analysis, devils-advocate). PostCopyHint. 6-phase analysis methodology. 8 new tests + 1 updated test. Docs: goal.md, architecture.md, CHANGELOG.md, todo.md."
    },
    {
      "task": "T128: Replace git shell-outs in sync.go with go-git/v6 (clone, add, commit, push, pull)",
      "description": "Replace exec.Command git calls in internal/command/sync.go with github.com/go-git/go-git/v6 library. Operations to convert: clone, add, commit, push, pull. Design constraints: prefer streaming file reads/writes directly from git objects (avoid temp files, avoid buffering entire files to disk), go-git API is 'fucky' - use pragmatically where it works, don't force it where it fights you. Preserve: pull uses --rebase strategy, push commits with timestamp message 'osm sync: <RFC3339>', conflict detection ('CONFLICT' or 'could not apply'). Exception: diff commands (osm ctx add --from-diff) continue using CLI git - no change. Update sync_test.go to work with go-git or mock appropriately.",
      "status": "Not Started"
    },
    {
      "task": "T129: Replace git shell-outs in sync_startup.go with go-git/v6 (auto-pull)",
      "description": "Replace exec.Command git call in internal/command/sync_startup.go with go-git. SyncAutoPull function runs git pull --rebase on startup when sync.auto-pull=true. Preserve non-blocking behavior, error handling to stderr. Use same go-git patterns as sync.go. Update sync_startup_test.go accordingly.",
      "status": "Not Started"
    },
    {
      "task": "T130: Verify sync tests pass after go-git migration",
      "description": "Run full sync test suite (internal/command/sync_test.go, internal/command/sync_startup_test.go) to verify all behaviors preserved: save/list/load, init/push/pull, auto-pull, path injection. Ensure no regressions. Coverage should remain high.",
      "status": "Not Started"
    },
    {
      "task": "T131: Remove unused sync.enabled config key",
      "description": "Remove sync.enabled from internal/config/schema.go (line 495) - it's defined but never used anywhere in the codebase. Also update any tests or documentation referencing it.",
      "status": "Done",
      "outcome": "Removed sync.enabled from schema.go, schema_test.go, benchmark_test.go, config.md, configuration.md, git-sync-design.md. CHANGELOG entry added. todo.md annotated. Also fixed pre-existing completion test missing which-one-is-better goal."
    },
    {
      "task": "T132: Verify TestSuperDocument_BacktabNavigation for flakiness",
      "description": "Run TestSuperDocument_BacktabNavigation multiple times in parallel (e.g., 5-10 iterations) and on Linux via Docker to verify stability. ANY flakiness MUST be fixed immediately. VERIFY FIXES by re-running the same parallel/Linux tests until NO flakes occur for minimum 5 consecutive runs. Document any changes made and confirm stability.",
      "status": "Done",
      "outcome": "Root cause: inconsistent inter-keystroke delays (4-20ms) across PTY integration tests caused character drops under CPU load. Fix: added ptyCharDelay constant (25ms) and replaced all 13 character-typing delays across both test files. 5/5 consecutive passes verified."
    },
    {
      "task": "T133: Module resolution for JS require()",
      "description": "Add ability to include JS modules directly. Need to pick module resolution/loader strategy. Revalidate understanding of current JS ecosystem standards. Want 'sane' subset tailored for specific use cases. Standards compliance ideal but don't rule out future interoperability. CURRENT STATE: Module resolution implemented via script.module-paths config key and require() in Goja runtime. Standards-compliant ESM NOT pursued. PARTIAL.",
      "status": "Partial",
      "outcome": "Module resolution via script.module-paths and require() implemented. Standards-compliant ESM (ECMAScript Modules with import/export) NOT pursued. This is a conscious scope limitation - full ESM support remains out of scope."
    },
    {
      "task": "T134: Streaming support for fetch API",
      "description": "Implement streaming support for fetch API. Requires eventloop-native in-process gRPC channel between JS client and Go server, or reactor-style HTTP client in Go backed by unpublished github.com/joeycumines/go-eventloop module. Expose compliant fetch API on top. Alternative: in-process gRPC channel implementation exposing both Goja JS API and Go-compatible API (sexiest option, avoids serialization). Full streaming support is complex - non-streaming already done in T028. PARTIAL - currently deferred.",
      "status": "Partial",
      "outcome": "Non-streaming fetch implemented (T028). Streaming support requires in-process gRPC channel or reactor-style HTTP client. Deferred due to complexity. Not blocking for current use cases."
    },
    {
      "task": "T135: VS Code .prompt.md file discovery and integration",
      "description": "Add support for https://code.visualstudio.com/docs/copilot/customization/prompt-files. .prompt.md file discovery via prompt.file-paths config key and goal system. VS Code-specific integration NOT pursued - only the file discovery mechanism. PARTIAL.",
      "status": "Partial",
      "outcome": ".prompt.md file discovery implemented via prompt.file-paths config key. VS Code-specific integration (editor integration, Copilot Chat integration) NOT pursued. Scope limited to file discovery only."
    },
    {
      "task": "T136: Code review splitter - LLM-dependent prompts",
      "description": "Code review splitter prompts seem particularly LLM dependent, stalled. Would be easier as proper workflow engine. Referenced in docs/todo.md as stalled. PARTIAL - basic chunking implemented (T124) but prompt refinement may be needed for different LLMs.",
      "status": "Partial",
      "outcome": "Diff splitting infrastructure complete (T124). LLM-specific prompt optimization remains open-ended. May require per-LLM prompt tuning or workflow engine integration."
    },
    {
      "task": "T137: MacOSUseSDK integration via gRPC",
      "description": "Evaluate potential integration with github.com/joeycumines/MacosUseSDK. In-process gRPC channel implementation would allow exposing gRPC APIs to JS code. Consider implementing gRPC proxy mechanism to expose MacOSUseSDK functionality to JS code. Requires T134 (in-process gRPC) first. NOT STARTED.",
      "status": "Not Started",
      "outcome": "Deferred pending T134 (in-process gRPC channel). MacOSUseSDK integration requires gRPC infrastructure."
    },
    {
      "task": "T138: Enhance definitions and integration with github.com/joeycumines/go-prompt",
      "description": "Enhance definitions and integration with go-prompt implementation. Details to be determined - reference existing go-prompt usage in codebase.",
      "status": "Not Started",
      "outcome": "Deferral pending clarification on specific enhancements needed."
    },
    {
      "task": "T139: Full Anthropic prompt library integration",
      "description": "Complete investigation and implementation of Anthropic prompt library (https://platform.claude.com/docs/en/resources/prompt-library/library). Currently PARTIAL (T074, T107) with morale-improver, bug-buster, code-optimizer, code-explainer, meeting-notes, pii-scrubber, prose-polisher. Additional prompts from library may be valuable.",
      "status": "Partial",
      "outcome": "6 Anthropic-inspired goals implemented. Full library coverage not complete - additional prompts may be added based on user demand."
    },
    {
      "task": "T140: Configuration model - further refinement",
      "description": "Continue iteration on configuration model for better extensibility and consistency. Schema-aware validation, persistence, and documentation added (T065, T082) but model may still need refinement based on usage.",
      "status": "Partial",
      "outcome": "Schema validation and persistence implemented. Configuration model considered 'undercooked' in docs/todo.md - may need further iteration based on real-world usage patterns."
    },
    {
      "task": "T141: Goal and script discovery - further refinement",
      "description": "Continue refinement of goal and script autodiscovery mechanisms. Basic path commands added (T109) but full prototype-to-production refinement may be needed.",
      "status": "Partial",
      "outcome": "osm goal paths and osm script paths commands added. Debug logging exists. Discovery mechanism marked as 'prototype status/needs attention' in docs/todo.md - may need production hardening."
    },
    {
      "task": "T142: Reconsider streaming fetch API - pragmatic approach",
      "description": "Re-evaluate whether full streaming fetch API (T134) is needed or if pragmatic alternatives exist. Consider: chunked responses, callback-based APIs, or simplified streaming interfaces. Full eventloop-native gRPC may be overkill for actual use cases.",
      "status": "Not Started",
      "outcome": "Requires review of actual use cases for streaming. May simplify or defer if non-blocking operations suffice."
    },
    {
      "task": "T143: AI Orchestrator - PTY spawning and terminal management",
      "description": "See docs/todo.md 'AI Orchestrator / Claude Code Integration (2026-02-17)'. Component 1: PTY Wrapper Module - extend osm:exec (or create osm:pty) to support spawning processes with full PTY. Terminal State Preservation, Signal Forwarding (SIGWINCH, SIGINT, SIGTERM), Input/Output Bridging. Reference: internal/scripting/pty_test.go shows PTY capability using github.com/creack/pty.",
      "status": "Not Started"
    },
    {
      "task": "T144: AI Orchestrator - MCP bidirectional data exfiltration",
      "description": "Component 2: Extend existing MCP server (internal/command/mcp.go) for bidirectional communication. Claude Code runs as MCP client connecting back to osm. Session identification for multiple instances. Tool exfiltration: Claude uses MCP tools to send structured data (split PR descriptions, test results) back to osm. Existing 6 tools (addFile, addDiff, addNote, listContext, buildPrompt, getGoals) provide foundation.",
      "status": "Not Started"
    },
    {
      "task": "T145: AI Orchestrator - PTY parsing for setup/init/error handling",
      "description": "Component 3: PTY output capture is mandatory for rate limit handling, error detection, context clearing, output verification. Parse for: rate limit prompts (detect and wait/handle), permission prompts (conservative rejection with reasons - be secure), model selection menus (ollama launch claude --config TUI wrapper), SSO login flows (aws-vault, etc.). Security principle: default to rejecting permission prompts with clear reasons.",
      "status": "Not Started"
    },
    {
      "task": "T146: AI Orchestrator - Configuration and environment variable management",
      "description": "Three-tier approach: (1) Shared with osm - Claude Code inherits osm's environment (API keys, MCP config), (2) Script Hooks - JavaScript scripts run hooks to inject env vars at spawn time (op plugin for 1Password, aws-vault for AWS), (3) Profile-Based Configuration - separate env var profiles in osm config with CLI flag overrides. Special handling for provider-specific commands (ollama launch claude --config).",
      "status": "Not Started"
    },
    {
      "task": "T147: AI Orchestrator - Provider abstraction (first pass: single instance)",
      "description": "Provider abstraction for AI orchestration. First pass: single instance support only. Future: modular, extensible design for multiple concurrent instances, different providers (GitHub CLI, Gemini CLI, Codium CLI), provider-specific configuration (ollama model selection TUI navigation). Design for extensibility from the start.",
      "status": "Not Started"
    },
    {
      "task": "T148: AI Orchestrator - Behavior tree orchestration engine",
      "description": "Use behavior trees (PABT) for high-level workflow control with prebuilt action templates. Behavior trees are INTERNAL implementation detail - not exposed to users. High-level orchestration: manage workflow structure. Action templates: prebuilt modes for common operations. Claude-centric execution: steps execute through Claude Code. Prompt-based control: prompts to Claude, MCP for communication back. Planning on failure: when step fails, LLM generates new plan with clear validation points and actionable remediation steps. Skills Integration: skills system provides domain knowledge for remediation.",
      "status": "Not Started"
    },
    {
      "task": "T149: AI Orchestrator - PR splitting branch structure and workflow",
      "description": "Specific use case: interactive PR splitting. Branch structure: linear series of branches, each with root (base branch), bottom-most based on trunk (original branch), hierarchy from outer to inner, each staged part is mergeable unit. Interactive Rebasing Workflow: interleaved AI assistance, branch rebased into components/commits, sanity verification in tandem with AI, user controls safety/verification/explicit checks. Equivalence Verification: final branch must represent original branch, split preserves semantics, user validates nothing lost unexpectedly. Non-negotiable - 'slop is bad'.",
      "status": "Not Started"
    },
    {
      "task": "T150: AI Orchestrator - TUI multiplexing with meta-key switching",
      "description": "osm acts as multiplexer swapping between its own TUI and external tool's TUI via meta-key switching (tmux-style). User is tmux user - must avoid conflicting with tmux keybindings. Terminal state management critical.",
      "status": "Not Started"
    },
    {
      "task": "T151: AI Orchestrator - Special-case TestMain for integration testing",
      "description": "Testing mechanism: (1) Spawn Claude command from osm, (2) Prompt with task instructions, (3) Response via MCP back to wrapping osm process. TestMain with CLI flags: configure provider(s) to test against, env vars for secrets (never hardcode), disabled by default (-test-integration flag), provider-specific configuration. Example: Ollama testing requires navigating TUI menu to select gpt-oss:20b-cloud model.",
      "status": "Not Started"
    },
    {
      "task": "T152: AI Orchestrator - Error recovery and cancellation",
      "description": "How should osm handle: Claude Code crashes, hangs, unexpected exits? Progress reporting from long-running operations? User cancellation of in-progress operations? Signal handling and cleanup critical.",
      "status": "Not Started"
    },
    {
      "task": "T153: AI Orchestrator - Session isolation for multi-instance",
      "description": "Future requirement: how should osm isolate multiple Claude Code sessions? Session identification, state management, resource isolation. Design for this from start even if first pass is single-instance only.",
      "status": "Not Started"
    },
    {
      "task": "T154: AI Orchestrator - Expose building blocks to users",
      "description": "Various building blocks must be exposed to users for their own implementations. Examples in scripts/ including for PA-BT. PTY spawning, MCP communication, behavior tree templates should all be accessible as modules/commands.",
      "status": "Not Started"
    },
    {
      "task": "T155: AI Orchestrator - Architectural design and approval",
      "description": "Complete architectural design phase with multiple implementation approaches (minimal changes, clean architecture, pragmatic balance). Compare trade-offs, create concrete implementation plan, seek user approval before implementation begins. This is the gate before T143-T154 implementation.",
      "status": "Not Started"
    }
  ],
  "continuousVerification": {
    "task": "Run 'make all' with full logging after EVERY significant change",
    "description": "Execute 'make make-all-with-log' after: (a) Each code change, (b) Each test addition, (c) Each task completion, (d) Each bug fix.",
    "failPolicy": "If ANY test fails, STOP IMMEDIATELY and fix the issue before proceeding. Zero tolerance for test failures, timing issues, non-determinism, or race conditions.",
    "testCoverage": "You will ALWAYS aim for 100% effective test coverage, and VERIFY test coverage by means of the tooling available to you. You will NOT proceed to the next task until you have achieved 100% effective test coverage for the current task, and have verified that coverage with the tools available to you."
  },
  "finalEnforcementProtocol": [
    {
      "type": "POST_EXECUTION_WARNING",
      "alert": "ON TOOLS: Use `config.mk` to create custom targets to run with Make. ALWAYS use custom targets that *limit* the amount of output you receive. For example, piping through tail, with FEW lines output. Prior to tail, pipe to tee. The file ./build.log in the root of the project is gitignored, so use that. That way you can *search* the output. To be clear, timing dependent tests are BANNED. As are those that take too long to run. Testing retries, for example, MUST be done in a way that supports avoiding running afoul of those CRITICAL rules. Abide. OBEY."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "ON TOOLS: Use `config.mk` to create custom targets to run with Make. ALWAYS use custom targets that *limit* the amount of output you receive. For example, piping through tail, with FEW lines output. Prior to tail, pipe to tee. The file ./build.log in the root of the project is gitignored, so use that. That way you can *search* the output. To be clear, timing dependent tests are BANNED. As are those that take too long to run. Testing retries, for example, MUST be done in a way that supports avoiding running afoul of those CRITICAL rules. Abide. OBEY."
    }
  ]
}
