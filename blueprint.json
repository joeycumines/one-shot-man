{
  "meta": {
    "fileName": "blueprint.json",
    "maintenanceInstruction": "KEEP THE PLAN UP TO DATE AS YOU EVOLVE THE CODE. DEVIATIONS TO THE PLAN MUST BE LOGGED WITHIN THE PLAN. THE PLAN MUST BE REASSESSED HOLISTICALLY AFTER ANY CHANGE OF ANY SIZE.",
    "completionPolicy": "YOU ARE NOT DONE UNTIL YOU HAVE COMPLETED 100% OF THE BLUEPRINT. DO NOT STOP. YOU MUST REFINE AT REGULAR INTERVALS TO KEEP THE BLUEPRINT FULL. NEVER SPECIFY ANY ESTIMATE OF EFFORT OR COST \u2014 ESPECIALLY NOT TIME ESTIMATES. NEVER SPECIFY ANY PRIORITY OF ANY KIND."
  },
  "globalAlerts": [
    {
      "warning": "Forks are 20260823 baseline (go-eventloop 22f9a6a, goja-eventloop 54cfe1a, goja 46200cb, goja_nodejs 05fa4bc) BUMPED to 20260824 patch set at cd0578a (go-eventloop 22f9a6a, go-inprocgrpc e327357, goja-grpc 39a74e7, goja-protobuf 9e31b568). Treat Adapter.Submit, Adapter.NewPromise+PromiseSettler, Adapter.Done, TrackAbortSignal, strict microtask always-on as ground truth. Legacy Loop/JS/Runtime/GojaWrapPromise/NewChainedPromise/WithStrictMicrotaskOrdering remain DELETED \u2014 grep gate must stay zero."
    },
    {
      "warning": "BOTH `gmake -j 10 make-all-with-log` AND `gmake -j 10 make-all-in-container` MUST pass with zero unintended failures \u2014 explicitly, no exceptions, on every platform tier the Makefile defines (all = build+vet+staticcheck+deadcode+test). Container target derives golang:$go_version from go.mod (currently 1.27.0); if docker manifest for golang:1.27.0 is absent, update example.config.mk (and config.mk) to pin an existing image (e.g., 1.27, 1.27-bookworm, or latest patch) and document fallback. Cross-platform make-all-run-windows is secondary but must not regress."
    },
    {
      "warning": "Tests must be isolated (t.TempDir, t.Setenv, no host-state mutation) and must not depend on loop-terminated state. Slow tests (>2s) guard with skipSlow/testing.Short. Never use build tags to segment tests."
    },
    {
      "warning": "Compliance harness (harness.js __activeSink) is load-bearing but does not cover host binding layer \u2014 regressions live in binding layer, not compliance specs. Do not claim compliance PASS means integration PASS."
    },
    {
      "warning": "Post-migration ZERO legacy hits: adapter.Loop()/JS()/Runtime()/GojaWrapPromise/WithStrictMicrotaskOrdering/NewChainedPromise/PromisifyFunc must grep zero in --include=*.go (allow CHANGELOG+blueprint history). Single async pattern is Adapter.NewPromise+Settler+goroutine+ctx or Loop.Promisify where *Loop held; fire-and-forget via adapter.Submit."
    },
    {
      "warning": "ALWAYS tee before tail: `mkdir -p scratch && <cmd> 2>&1 | tee scratch/<name>.log | tail -n 100; echo EXIT:$PIPESTATUS` \u2014 never `gmake ... | tail` without tee."
    }
  ],
  "mandatoryDirectives": {
    "contextManagement": "Tasks are flat sequential, one in_progress at a time. Every session starts cold by reading blueprint.json + WIP.md + build.log + scratch/engine-slop-autopsy/README. Update task status incrementally after each internal checkpoint.",
    "eventloopIntegrationPolicy": "Every fork capability adopted must LEVERAGE new semantics, not merely patch compile. WithLogger/WithMetrics/Loop.Log give panic observability; Requests/SetQuiescenceHandler give liveness correctness; strict microtask always-on removes option plumbing. Document deliberately unused surface (RegisterFD) with rationale.",
    "completeness": "Every deficiency in autopsy 02-05,10 is owned: sandbox, tokenizer race, fetch streaming+liveness, Bridge nil-adapter class, Bridge stall, termmux/grpc/pabt red lists, scripting races, loop monopolization, spawn/disk sync, context severance, settlement slop. No 'pre-existing' noise \u2014 main at 498102f proves all 6 packages green before branch.",
    "standardGoTestPatterns": "Standard Go only: -run patterns, testing.Short/skipSlow, t.Skip only for platform guards (Windows PTY). No custom flags/TestMain/build tags.",
    "quantifiedCompliance": "Quantified compliance retained (test262 1100 + goja_compat 600 via go:embed) but tail is CLOSED \u2014 pass rate vs goja baseline reported, not expanded indefinitely. Binding-layer regressions are the critical path, not language compliance optimization.",
    "containerDualTarget": "Dual-target mandate: local `gmake -j 10 make-all-with-log` and container `gmake -j 10 make-all-in-container` (docker golang:$go_version) both green. If image missing, update example.config.mk fallback (strip patch, use bookworm, or pin latest) and verify `docker run --rm golang:$ver go version` succeeds before marking container task Done.",
    "strictReviewGate": "Rule of Two applies per task and once globally. Two contiguous hostile independent reviews must guarantee correctness on same unchanged diff; any fix resets the pair. Reviews verify implementation generalizes, not hard-coded to test inputs.",
    "ALWAYS": "Tee every long command to scratch/ before tail/head per globalAlerts. Pattern: mkdir -p scratch && <cmd> 2>&1 | tee scratch/<name>.log | tail -n 100; echo EXIT:${PIPESTATUS[0]}"
  },
  "statusSection": {
    "currentState": "Task 8/22 DONE: scripting race fixed (SetGlobal/GetGlobal owner-safe). Verified via -race no race, grep shows only loop paths. Next: task 9 F3/F4 TUIManager.Run/SwitchMode.",
    "goalState": "All 6 red packages green, sandbox escape closed, fetch streaming restored with liveness, Bridge nil-adapter class eliminated, Bridge stall gone (5s+forever -> sub-ms), tokenizer race gone, scripting races gone, binding contract converged (zero loop-monopolizing sync twins), duplication collapsed (~350+80+60 lines net deletion), stale microtask comments fixed, WithLogger/Metrics wired, verification parity (-race/fuzz/determinism/coverage) green, docs/AGENTS realigned, and BOTH `gmake -j 10 make-all-with-log` and `gmake -j 10 make-all-in-container` pass with zero unintended failures (go vet/build/deadcode/staticcheck + `go test -timeout=20m ./...`). Two contiguous strict-review-gate passes confirm no hard-coded test-only fixes.",
    "goalLog": [
      {
        "entry": 1,
        "timestamp": "2026-07-03T17:00:00+10:00",
        "direction": "Exhaustive JS-compliance: every deviation from external standards (ECMAScript/WHATWG/Node) must bubble as a GO TEST FAILURE in internal/jscompliance. The user's directive: 'implement proper JavaScript-native APIs, to EXHAUSTIVELY comply with external standards for behavior and execution and semantics, and ensure the osm scripting engine is subject to automated tests leveraging compliance verification testing suites, with non-compliance of ANY KIND bubbling as GO TEST FAILURES.' The user steered: do NOT touch the eventloop fork (WIP refactor in progress); fork-blocked gaps become failing tests, not skips. USE blueprint.json as persistent memory.",
        "decisions": [
          "Harness integrity is the linchpin: a name-keying bug in harness.js silently recorded failing assertions as PASS when test-name != assert-name. Fixed via sequential test execution + __activeSink (a single per-test failure collector). 269 assert call-sites across 12 specs were affected.",
          "Spec bugs (wrong expectations) are fixed to match correct engine behavior: Reflect is an object not function; regexp.find needs unanchored pattern; flag.visit callback receives {name,...} not a bare string; lipgloss.size returns {width,height}; bubbletea.isValidTextareaInput returns {valid,reason}; log.Level is exported as an object (String() => 'INFO'); regexp.replace replaces first-only; bt.tick is an async state machine (first tick=running, second=settled).",
          "Real in-repo engine bug fixed: nextIntegerID initialized maxVal=0, causing all-negative ID lists to return 1 instead of max+1=0. Fixed via math.MinInt64 init + empty-list guard.",
          "Fork-blocked gaps (Symbol.asyncIterator, abort(reason), RISK-A unhandled rejection) are now FAILING tests (not skips) with documented fix paths.",
          "WAIT-1 (termmux.wait sync) is FIXABLE-IN-REPO (termmux package is in this repo) \u2014 failing test active, fix is a blueprint task."
        ]
      },
      {
        "entry": 2,
        "timestamp": "2026-07-27T11:25:00+10:00",
        "direction": "Integrate 3-Stage Heterogeneous Adversarial Code Review Pipeline into osm CLI (one-shot-man) under . Pipeline features Stage 1 Micro-Diff Filter, Stage 2 AST & Call-Graph Context Packager (Tree-Sitter + LSP), Stage 3 Blinded Dual-Auditor Consensus (Open-Weights Red-Teamer vs Closed Reasoning Verifier) + Arbiter dispute resolution, and Interactive TUI Verification Pane.",
        "decisions": [
          "Go engine infrastructure added under internal/builtin/astpack/ and internal/command/diff_triage.go, exposing osm:astpack and osm:diff_triage to JS via non-blocking Promisify bindings following AGENTS.md contract.",
          "Multi-provider LLM Router added under internal/builtin/llmrouter/, supporting prompt caching stacking, Batch API 24h SLA, sticky routing, and ZDR privacy headers.",
          "Workflow orchestration implemented in embedded code_review_3stage.js with interactive TUI pane (code_review_tui.js) and compliance tests in internal/jscompliance/specs/code_review_3stage.spec.js."
        ]
      },
      {
        "entry": 3,
        "timestamp": "2026-07-27T14:50:00+10:00",
        "direction": "Replan: Complete remaining JS compliance work (WAIT-1 fix, CommonJS behaviors, Web-API gaps, doc-drift resolution) and restructure the build pipeline so that fork-blocked test failures no longer break the main build target (gmake all). Fork-blocked tests remain as failing tests (not skips, not tolerated) but are separated into their own test target using standard Go `-run` name patterns (idiomatic Go testing, no custom flags, no TestMain infrastructure, no build tags). The main test pipeline (gmake all, test-jscompliance, test-jscompliance-all) excludes fork-blocked tests via `-run` name patterns in config.mk. A dedicated test-jscompliance-fork-blocked target uses a `-run` pattern that includes only fork-blocked test functions (expected to fail independently). After compliance is complete and the build pipeline passes on all three platforms, proceed to the 3-Stage Pipeline feature work.",
        "decisions": [
          "Fork-blocked test failures (GOJA-FORK, ADAPTER-FORK, EVENTLOOP-FORK) are correct and must remain as failing tests. They are not skipped and not tolerated \u2014 they are separated into a dedicated test target that is expected to fail, using standard Go `-run` name patterns.",
          "WAIT-1 (termmux.wait sync) is FIXABLE-IN-REPO and must be fixed as part of compliance completion. After the fix, termmux.wait() returns a Promise via Promisify, mirroring exec.spawn.wait() async pattern.",
          "The build pipeline (gmake all) currently fails because go test ./... includes the jscompliance package which has fork-blocked test failures. The fix: move fork-blocked assertions to separate spec files and Go test functions, and exclude them from main test runs using standard Go `-run` name patterns in config.mk (negative lookahead to exclude fork-blocked test function names).",
          "AGENTS.md mandates standard Go testing patterns for test separation: `-run` name patterns for test selection, `testing.Short()` guards for slow/platform tests, and `t.Skip()` only for platform-specific guards (e.g., Windows PTY). No custom flags, TestMain fork infrastructure, or ad-hoc test-control mechanisms are permitted.",
          "Compliance work is a prerequisite for the 3-Stage Pipeline feature work. The 3-Stage Pipeline tasks remain in the blueprint but are sequenced after compliance completion.",
          "The Rule of Two (strict-review-gate) applies to all changes including the Makefile restructuring, spec file reorganization, and WAIT-1 fix."
        ]
      },
      {
        "entry": 4,
        "timestamp": "2026-08-24T08:00:00+10:00",
        "direction": "MAXIMUM-EFFORT INTEGRATION of the critical dependency update (go 1.27.0, go-eventloop 673d434, goja-eventloop 54cfe1a, goja 46200cb, goja_nodejs 05fa4bc, charm bubbletea 2.0.9/lipgloss 2.0.6/bubbles 2.2.0, go-git alpha.5, etc.). The breaking changes were SPECIFICALLY for this project's benefit: owner-topology scheduler, exact Node v26.5.0 timer/nextTick/microtask/Promise semantics, Promise.withResolvers/try, AbortSignal.any/timeout with ownership, performance origin, structuredClone DataCloneError, timer handle ref/unref/dispose, quiescence, Done barrier, bounded retention, and strict microtask ordering now always-on. The integration must not be a minimal patch \u2014 it must comprehensively adopt every new capability that improves correctness, observability, and robustness, promote newly-fixed fork-blocked tests, and expand coverage. AGENTS.md must be realigned to the new integration surface while preserving intent. A continual background scan for further alignment gaps is mandated.",
        "decisions": [
          "Exhaustive migration from legacy Adapter.Loop/JS/Runtime/GojaWrapPromise + JS.NewChainedPromise + Loop.Promisify to Adapter.NewPromise+PromiseSettler + Adapter.Submit, with goroutine offloading and context propagation via captured baseCtx. Loop.Promisify retained only where *goeventloop.Loop is directly held (runtime.go bootstrap token). PromisifyFunc signature is retired.",
          "WithStrictMicrotaskOrdering deleted everywhere (runtime.go, testutil/eventloop.go, termmux pane tests, docs). New strict ordering is always-on; new compliance tests in core_microtask.spec.js verify ordering without option.",
          "No shims retained per Internal API Discipline: one implementation, update everything, delete boldly. A grep gate (zero legacy adapter calls) pins the migration.",
          "AGENTS.md Concurrency Model and JS Binding Contract updated to describe Adapter.Submit(func(*goja.Runtime)) and Adapter.NewPromise+Settler, context threading via baseCtx, and Done/TrackAbortSignal where applicable \u2014 preserving clipboard-first, platform compat, and logging conventions.",
          "Compliance expansion: re-probe all prior GOJA-/ADAPTER-/EVENTLOOP-FORK markers against the new runtime; any now-passing assertion (e.g., Symbol.asyncIterator, abort reason, isWellFormed/groupBy, unhandledRejection) is promoted from fail to pass and moved to the main pipeline. New surfaces (withResolvers/try, any/timeout, structuredClone DataCloneError, timer dispose) get failing-then-passing specs as appropriate.",
          "Charm and go-git bumps audited as first-class integration tasks (bubbletea key pipeline, lipgloss width/height, bubblezone hit-testing, go-billy alpha.2, securejoin removal). Build must pass on Linux/Windows via cross-build after each tier.",
          "Existing pending work (Complete JS Compliance Suite, fork-blocked separation, 3-Stage Pipeline Core/Dual-Auditor/TUI, Rule of Two) remains as the exhaustive tail, sequenced after integration is green, with dependencies rewired to the new adapter contract."
        ]
      },
      {
        "entry": 5,
        "timestamp": "2026-08-24T11:30:00+10:00",
        "direction": "REPLAN: Prune even the always-expand-scope task. Intent is QUANTIFIED JAVASCRIPT COMPLIANCE \u2014 maximally compatible JS engine behavior BETTER than goja and robust and correct. Already have compliance tests (internal/jscompliance harness.js + specs via go:embed, sequential __activeSink). Ideally integrate external tests, similarly to ~/dev/goja/ (tc39_test.go with test262 harness, builtin_*_test.go suites). The task list is closed \u2014 no indefinite expansion. Drive to zero outstanding items and a quantified pass-rate vs goja baseline.",
        "decisions": [
          "Remove the indefinite-cycle mandate (backgroundGapDiscovery + continual gap sweep task). The tail is now closed and quantified: external test integration + gap closure to surpass goja + robustness + reporting.",
          "Adopt goja's external test pattern: testdata/test262 harness (tc39_test.go:879, skipList, harness, yaml, semver) and builtin test suites (builtin_array_test.go, builtin_promise_test.go etc.) as reference for quantified compliance. Reuse goja's harness concepts: prefixList, skipList, ignorableTestError, yaml frontmatter, semver.",
          "Define quantified goal: pass rate vs goja baseline on same fork surface (20260823), with CI reporting (json/markdown) and coverage of all newly-exposed surfaces (withResolvers/try, any/timeout, DataCloneError, dispose).",
          "Prune scope: delete 'Continual background gap discovery and scope expansion sweep' from sequentialTasks entirely. Update mandatoryDirectives: backgroundGapDiscovery -> quantifiedCompliance (closed list). Update goalState/currentState to reflect quantified compliance.",
          "Keep Done work (harness fix, spec bugs, nextIntegerID, fork-blocked pins, WAIT-1, ES2022-2025, AGENTS.md realignment, strict ordering, canonical async, per-module migrations, zero-legacy gate, re-probe, new-surface specs, charm/git bump, doc drift, JS compliance remainder, fork separation, T22 Core) as is. Parked consensus engine (T23/T24) remains in docs/todo.md and is NOT re-added."
        ]
      },
      {
        "entry": 6,
        "timestamp": "2026-08-24T11:35:00+10:00",
        "direction": "DIRECTIVE: NO POLYFILLS. User clarified: 'Better than goja' must be achieved via go-eventloop / goja-eventloop modules, NOT via JS polyfills/shims in engine_core.go. Polyfills for isWellFormed/toWellFormed, Object/Map.groupBy, Symbol.asyncIterator are FORBIDDEN. The quantified compliance tail must rely on native fork surface to surpass goja. Any prior plan to shim ES2024 features is revoked.",
        "decisions": [
          "Kill internal/scripting/polyfills.go and its RunString injection in engine_core.go. Removed ES2024 polyfill block that injected isWellFormed/toWellFormed/groupBy/asyncIterator via JS.",
          "Retain core_es_fork_blocked.spec.js as FAILING fork-blocked tests (3 failures) in test-jscompliance-fork-blocked target; do not promote via polyfill. 'Better than goja' is proven via quantified test262 (1100 cases, 100%) and goja-compat (600 cases, 100%) suites where osm >= goja on 20260823 surface, not via shimming missing ES2024 symbols.",
          "Update task 'Close remaining fork-blocked gaps to surpass goja' to forbid shims: fixes must be in goja/goja-eventloop/go-eventloop forks or remain as documented ADAPTER-FORK-BLOCKED fails; JS polyfills are not an acceptable fix path.",
          "Record this directive in blueprint.json per user request; append to replanLog as Replan 5 for auditability."
        ]
      },
      {
        "entry": 7,
        "timestamp": "2026-08-24T12:01:54.663015",
        "direction": "DIRECTIVE: Adapter.NewPromise audit \u2014 go doc -all go-eventloop / goja-eventloop mandatory. Adapter.NewPromise is extremely questionable; if Go-resolvable promise needed, existing Loop.Promisify / Requests / Done / TrackAbortSignal abstractions must be used. FULL migration to best surfaces mandatory, DO NOT tolerate cruft. Leverage AbortController, sophisticated Go-native vs goja-integration functionality. Fan out subagents and integrate ALL refinement areas to bring one-shot-man API surface/concurrency in line with optimal latest refined packages; cull MANY HACKS.",
        "decisions": [
          "Run go doc -all for both modules, read goja-eventloop/README, promisebridge, adapter, and Loop lifecycle; identify owner-only NewPromise vs Loop.Promisify distinction (JS-visible vs Go-native).",
          "Audit all 17 Adapter.NewPromise sites + async.Promise helper vs Loop.Promisify (context propagation, Goexit handler, liveness via promisifyWg, graceful Shutdown tracking, fallback).",
          "Introduce canonical helper that leverages Loop.Promisify where *Loop is held (runtime.go bootstrap) and bridges to JS promise via settler only where JS-visible; elsewhere use adapter.NewPromise only for true JS promises, with TrackAbortSignal for AbortController.",
          "Cull manual goroutine+settler boilerplate, manual ErrPromiseSettled/ErrAdapterInvalid guards, and ad-hoc context threading; replace with loop-native abstractions.",
          "Record in blueprint.json per directive; fan out explore/librarian subagents for exhaustive refinement."
        ]
      },
      {
        "entry": 8,
        "timestamp": "2026-08-25T01:50:54.438709+10:00",
        "direction": "EXHAUSTIVE REPLANNING: engine-slop autopsy (build.log 2026-08-25 + scratch/engine-slop-autopsy/ 01-12 + evidence) proves blueprint 33/33 Done is false \u2014 tree is red in 6 packages (internal sandbox 3, fetch 4, grpc 6, pabt 1, termmux 7, jscompliance 1) including SECURITY sandbox escape process.exit, deterministic fetch body-streaming dead, nil-adapter class, Bridge.RunSync 5s/forever stall, tokenizer off-loop VM race, scripting races, loop-monopolizing sync twins, context severance, settlement slop, 6-way wait-for-loop duplication, stale microtask comments, unused WithLogger/Metrics. Mandate: FULLY ADDRESS all deficiencies known and unknown, present and future, and make BOTH `gmake -j 10 make-all-with-log` and `gmake -j 10 make-all-in-container` MUST pass (update container Go version pin in example.config.mk if golang:1.27.0 image missing). No scope hand-waving, no pre-existing noise \u2014 every failure is owned.",
        "decisions": [
          "Treat build.log as ground truth, not WIP claims: 6 red packages at cd0578a (go-eventloop 22f9a6a/b, goja-grpc 39a74e7, bubbles 2.2.1) \u2014 not 5 at 80e5a3e. Add jscompliance TestBindingContract_IOExportsAreAsync loop-terminated to red list.",
          "Phase 0 stop-the-bleeding front-loads small structural fixes (sandbox, tokenizer settler, fetch adapter+ctx+liveness, Bridge adapter required, Bridge fast path, termmux ownership/wait, grpc loop callback, scripting race) \u2014 each lands with its package -race green before next.",
          "Collapse duplication aggressively but regression-free: one Runner substrate for wait-for-loop sextet + lifecycle triplet delegation; unify bt leaf/composite/ticker; delete dead _any params and state-refresh storm.",
          "Converge binding contract: delete sync twins, make every I/O binding Promise+goroutine+ctx+owner-safe settler, fix context severance and settlement slop \u2014 contract stops being folklore.",
          "Leverage fork capability half: wire WithLogger (Loop.Log) and WithMetrics behind slog so panics become diagnosable events and perf mandate becomes measurable; adopt Requests/quiescence where needed.",
          "Harden verification permanently: -race tier, async helper tests, tokenizer first tests, bt fork-from-loop tests, fetch suite promotion, fuzz/determinism/coverage; docs and AGENTS realigned; container Go version verified against docker manifest and example.config.mk updated if needed.",
          "Final gate is strict-review-gate x2 contiguous plus full `gmake -j 10 make-all-with-log` and `gmake -j 10 make-all-in-container` green \u2014 both are mandatory and explicit in acceptance."
        ]
      }
    ],
    "replanLog": [
      {
        "event": "Replan 1",
        "timestamp": "2026-07-27T14:50:00+10:00",
        "trigger": "User directive: complete Node.js compliance work before 3-Stage Pipeline starts; build must pass on all three platforms; fork-blocked tests are correct failures to be separated not skipped",
        "affectedTasks": "All Not Started tasks in sequentialTasks restructured; 4 remaining compliance tasks (WAIT-1 fix, CommonJS, Web-API gaps, doc-drift) consolidated into task 1; new Task 2 added for fork-blocked test separation and build pipeline restructuring; 3-Stage Pipeline tasks sequenced after compliance as Tasks 3-5; Rule of Two preserved as Task 6",
        "summary": "Consolidated remaining compliance work into Task 1 (WAIT-1 fix, CommonJS, Web-API gaps, doc-drift). Added Task 2 (fork-blocked test separation using standard Go `-run` name patterns in config.mk, move fork-blocked assertions to separate spec files and dedicated Go test functions). Tasks 1\u20132 are staged/blocking prerequisites for 3-Stage Pipeline feature work (Tasks 3\u20135). Task 6 (Rule of Two) gates all changes."
      },
      {
        "event": "Replan 2",
        "timestamp": "2026-08-24T08:30:00+10:00",
        "trigger": "MANDATE: Maximum-effort integration of critical dependency update (go 1.27.0 + 20260823 fork surface) with breaking changes. User: 'Those breaking changes - one and all - were SPECIFICALLY for use by this project. Consider DEEPLY what that means, and maintain a continual background effort to identify further gaps.' Plus AGENTS.md realignment required.",
        "affectedTasks": "Full exhaustive replan. Preserved 6 Done tasks (harness, spec bugs, nextIntegerID, fork-blocked pins, WAIT-1 pin, ES2022-2025). Inserted new Task 7 (AGENTS.md realignment) + Task 8 (strict-ordering removal) + Task 9 (canonical async helper) + Task 10\u201318 (per-module exhaustive migration to NewPromise+Settler, zero-legacy gate) + Task 19 (re-probe and promote fork-fixed gaps) + Task 20 (new-surface compliance expansion) + Task 21 (charm/git bump audit) + Task 22 (doc drift). Rewired prior pending tasks 23\u201327 as tail (Complete JS Compliance Suite remainder, fork separation, 3-Stage Core, Dual-Auditor, TUI, Rule of Two) with dependencies on integration green. Total sequentialTasks now 28 (6 Done + 22 Not Started).",
        "summary": "Integrated the breaking dependency surface as the highest-effort front of the plan: AGENTS.md realignment first, then strict-ordering cleanup, then a single canonical async pattern, then exhaustive per-module migration (os/path/exec/ctxutil/fetch/gitops/tokenizer/termmux/aimux/mcp/mcpcallback/output/testutil), then zero-legacy grep gate, then compliance promotion and expansion with new adapter capabilities, then charm/git audits, then tail work (remaining compliance, fork separation, 3-Stage Pipeline). Every migration task has project-specific acceptance and context pointers; no estimates/priorities; flat sequence; continual gap discovery mandated."
      },
      {
        "event": "Replan 3",
        "timestamp": "2026-08-24T09:15:00+10:00",
        "trigger": "Holistic reassessment triggered by user directive: plan materialization does not take existing task list as gospel. Structural error uncovered (Task 20 pointed at non-existent internal/scripting/require/resolve.go:141; actual resolution lives in external goja_nodejs require/resolve.go + internal/scripting/engine_core.go registry). Grand scope tail (3-Stage Pipeline T22-24) required re-validation for depth and dependency on T20.",
        "affectedTasks": "Task 20 (Complete JS Compliance Suite remainder) contextPointers/description corrected; Task 22-23 dependsOn extended to include T20; statusSection.currentState updated. No sequentialTasks added/removed \u2014 tail remains T22-26, sequenced after integration green. Verified Tasks 22-24 pass anti-vaporware test (project-specific files, acceptance describes executable value).",
        "summary": "Corrected vaporware pointer, revalidated grand scope materialization (integration green -> compliance remainder -> 3-Stage Core/Dual-Auditor/TUI -> gap sweep -> Rule of Two). Plan now provably achieves goalState with zero gospel assumptions."
      },
      {
        "event": "Replan 4",
        "timestamp": "2026-08-24T11:30:00+10:00",
        "trigger": "User directive: completely replan the blueprint, PRUNE even the always-expand-scope task, INTENT: QUANTIFIED JAVASCRIPT COMPLIANCE maximally compatible JS engine BETTER than goja and ROBUST and CORRECT, integrate external tests similarly to ~/dev/goja/",
        "affectedTasks": "Removed 'Continual background gap discovery and scope expansion sweep' (Not Started) from sequentialTasks entirely (tasks 26->24, then to be expanded with quantified compliance tasks). Updated mandatoryDirectives: backgroundGapDiscovery removed, quantifiedCompliance added (closed list, no indefinite expansion). Updated statusSection.currentState/goalState and appended goalLog entry 5. Consensus engine (T23/T24) remains stripped and parked in docs/todo.md (not re-added).",
        "summary": "Pruned indefinite-cycle task, closed scope, replanned tail to quantified JS compliance: external test262 + goja builtin suites integration, gap closure to surpass goja (> baseline), robustness (fuzz/race/determinism), quantified reporting. New tail will be 5-6 Not Started tasks + Rule of Two, flat sequential, no estimates/priorities, each with acceptance, files, contextPointers, dependsOn."
      },
      {
        "event": "Replan 5",
        "timestamp": "2026-08-24T11:35:00+10:00",
        "trigger": "User directive: NO POLYFILLS. Kill polyfills; 'better than goja' via go-eventloop/goja-eventloop modules, not JS shims.",
        "affectedTasks": "Removed internal/scripting/polyfills.go and engine_core.go polyfill injection. Updated sequentialTasks task 'Close remaining fork-blocked gaps to surpass goja' description/acceptance to forbid shim/polyfill fix path; gaps remain as fork-blocked fails unless fixed in forks. Updated statusSection.currentState/goalLog.",
        "summary": "Polyfills deleted, blueprint task now explicitly forbids shim fix path, quantified 'better than goja' remains via test262/goja-compat native surface (1100+600 cases, 100% vs 98.5% baseline). Fork-blocked ES2024 gaps (isWellFormed, groupBy, asyncIterator) remain as 3 documented fails in fork-blocked target until fork fixes land."
      },
      {
        "event": "Replan 6",
        "timestamp": "2026-08-24T12:01:54.663024",
        "trigger": "User directive: audit Adapter.NewPromise via go doc -all, migrate fully to optimal Loop.Promisify/TrackAbortSignal surfaces, cull hacks, fan out subagents.",
        "affectedTasks": "All async promise sites in internal/builtin/* (async/promise.go, exec, fetch, termmux, etc.) and runtime bootstrap; tasks remain Done but flagged for refinement sweep.",
        "summary": "Recorded audit directive, launched 5 parallel subagents (go-eventloop docs, goja-eventloop docs, concurrency hacks, librarian eventloop/abort), prepared to integrate refinements via Loop.Promisify where *Loop held and TrackAbortSignal for abort."
      },
      {
        "event": "Replan 7",
        "timestamp": "2026-08-25T01:50:54.438709+10:00",
        "trigger": "ANALYZE-MODE: read build.log (392 lines, 22 FAILs across 6 packages) + ALL 12 autopsy docs + evidence/REPORT.md + repro harnesses. Prior blueprint 33/33 Done falsified \u2014 WIP 'no unresolved findings' blind, verification never ran red packages, branch cannot merge. Instruction: exhaustively replan to cater for ALL deficiencies both known and unknown, present and future, encode specifics, immediately execute, and make BOTH make-all-with-log and make-all-in-container MUST pass (update container Go version if needed).",
        "affectedTasks": "Full sequentialTasks replaced: 33 Done -> 22 Not Started exhaustive tasks (Phase 0 repair 1-8, contract convergence 9-13, substrate collapse 14-16, observability 17, verification parity 18-19, charm/docs 20, container dual-target 21, Rule of Two 22). globalAlerts/mandatoryDirectives/continuousVerification/finalEnforcementProtocol rewritten to pin dual-target mandate. goalLog entry 8 appended; prior 1-7 preserved append-only.",
        "summary": "Replanned from first-hand code + log evidence, not claims. New blueprint is closed, falsifiable, and proves 'done' via two hostile reviews plus both make targets green with zero unintended failures."
      }
    ]
  },
  "sequentialTasks": [
    {
      "task": "H0 SECURITY: neutralize fork new process globals so TestSandbox_* pass (process.exit/ env/pid/ Buffer/Deno/require bypass)",
      "description": "Engine hardening predates 20260823 goja-eventloop Bind which installs Node v26.5 process lifecycle globals (process.exit/exitCode, process.env/pid, Buffer, Deno). Fix in internal/scripting/runtime.go after adapter.Bind(): on-loop VM scrub \u2014 `vm.Set(\"process\", undefined)` override with stub where `exit` throws TypeError sandbox, or delete via `vm.Get(\"process\")` Object Delete + freeze; likewise neutralize `global.process`, `Buffer`, `Deno`, `exit`. Also harden module_hardening.go to block osm: prefix bypass reaching these globals. Pin with TestSandbox_NoGoUnsafe/NoOsExit/GojaDefaultsAreSafe (security_sandbox_test.go:90-187). Verify no legitimate script needs real process.exit \u2014 osm never exposes it intentionally.",
      "acceptance": "A user script executing `process.exit(1)`, `process.exit()`, `typeof process.exit==='function'` or `require('osm:os').exit` inside `scripting.NewEngine` either throws TypeError sandbox or is undefined, never terminates host. `go test -run TestSandbox -count=1 ./internal -race` green (3/3). `go test -run TestSecurity -count=1 ./internal/scripting` still green. Production `osm code-review` / `osm script -e 'process.exit'` no longer kills CLI; console output shows handled error.",
      "status": "Done",
      "files": [
        "internal/scripting/runtime.go",
        "internal/scripting/module_hardening.go",
        "internal/security_sandbox_test.go"
      ],
      "contextPointers": {
        "bindInstallsProcess": "goja-eventloop doc *Process APIs (Bind installs process.exit/exitCode)",
        "sandboxTests": "internal/security_sandbox_test.go:90-187",
        "runtimeBind": "internal/scripting/runtime.go:124-134"
      }
    },
    {
      "task": "H1 CRITICAL: fix tokenizer.loadFile off-loop VM mutation (only off-loop VM write in tree)",
      "description": "internal/builtin/tokenizer/tokenizer.go:97-103 does `return newTokenizerWrapper(runtime, tok)` inside async.Promise goroutine \u2014 concurrent with loop owner mutating same goja.Runtime (NewObject/Set/ToValue). Fix via settler callback: `go func(){ tok,err:=LoadTokenizerFile(path); if err { settler.Reject(rt.NewGoError) } else { settler.Resolve(func(rt *goja.Runtime) any { return newTokenizerWrapper(rt,tok) }) } }` and drop async.Promise helper here (its fn returns any outside owner). Keep path==\"\" immediate Null via NewPromise+Resolve. Delete direct runtime capture. Patch sketch P1 (12_phase0_patch_sketches.md) is the reference form.",
      "acceptance": "Concurrent `loadFile` while timers/microtasks run no longer races (go test -race ./internal/builtin/tokenizer with first-ever test file + hammer of 2 goroutines x 3s shows no race, `grep -R \"newTokenizerWrapper(runtime\"` zero). Existing interfaces (loadJSON/BPE/WordPiece remain sync) unchanged. `go vet` clean; new tokenizer_test.go with loadFile success+error+concurrent hammer passes -race.",
      "status": "Done",
      "files": [
        "internal/builtin/tokenizer/tokenizer.go",
        "internal/builtin/async/promise.go"
      ],
      "contextPointers": {
        "bugSite": "internal/builtin/tokenizer/tokenizer.go:97-103",
        "wrapper": "tokenizer.go:170-192",
        "asyncHelper": "internal/builtin/async/promise.go:22"
      }
    },
    {
      "task": "Regression 1 + H5 + H8: restore fetch body streaming with adapter+ctx threading, liveness, Background fix and dead _any removal",
      "description": "buildResponse (fetch.go:207-208) currently `NewReadableStream(Background, NopCloser(bytes.NewReader(body)), nil)` + `wrapReadableStreamJS(Background, runtime, nil, stream, nil)` \u2014 nil adapter panics in readable_stream.go:163-174 async.Promise nil, loop recovers panic -> never-settled Promise -> 10s hang (4 tests). Fix: (a) thread captured ctx+adapter from Require(ctx,adapter) through buildResponse(ctx, runtime, adapter, resp, body) and into NewReadableStream(ctx, src) + wrapReadableStreamJS(ctx, rt, adapter, rs); delete both `_ any` corpse params (readable_stream.go:37,138). (b) Restore per-pending-read liveness: each `reader.read()` holds a Loop.Promisify token (via eventLoopProvider Liveness interface) released on settle/cancel, matching Node ref semantics. Minimal form in P2 (12_...md): Promisify holds token while blocking Read, settler resolves inside same Promisify callback. (c) Replace Background with captured ctx for cancellation propagation. Wire via register.go already having adapter.",
      "acceptance": "Fetch body streaming works: `TestE2E_ReadableStream_ChunkedBody`, `TestResponseBody_GetReader_ReadAll`, `TextAndBodyIndependent`, `EmptyBody` all pass without timeout, -race clean. `wrapReaderJS` read() returns {value,done} Promise settled on owner, not panicking. Pending read holds loop alive (no premature auto-exit). `grep -n \"_ any\" internal/builtin/fetch/readable_stream.go` zero, `grep -n Background internal/builtin/fetch/fetch.go` shows no Background in body-stream path.",
      "status": "Done",
      "files": [
        "internal/builtin/fetch/fetch.go",
        "internal/builtin/fetch/readable_stream.go",
        "internal/builtin/register.go"
      ],
      "contextPointers": {
        "buildResponse": "internal/builtin/fetch/fetch.go:207-208",
        "wrapReader": "readable_stream.go:163-174",
        "asyncPanic": "internal/builtin/async/promise.go:18-20",
        "loopPanicDoc": "go-eventloop doc: callback panic recovered and logged"
      }
    },
    {
      "task": "H4 NIL-ADAPTER CLASS: require adapter at Bridge construction, delete SetAdapter, update pabt/bt helpers to one path (P3)",
      "description": "Bridge currently has `NewBridge(ctx,loop,vm,reg,adapter)` that stores nil plus `SetAdapter(adapter)` late-binding; nothing requires either, so misuse panics deferred to first .done() (pabt require_test.go:45 `NewBridge(...,nil)`). Fix: add `if adapter==nil { panic(\"adapter must not be nil\") }` at NewBridge top, delete SetAdapter method entirely, make construction-time requirement unrepresentable. Update call sites: internal/builtin/register.go:143 already passes real adapter (no change); migrate tests: pabt/require_test.go:45 -> `NewBridge(ctx,loop,vm,reg,adapter)` (adapter already built 3 lines above), bt bridge_test.go +2 SetAdapter lines -> pass adapter directly, benchmark_throughput_test.go, integration_test.go similarly. Grep `SetAdapter` must go zero.",
      "acceptance": "Nil adapter becomes impossible at construction, not deferred panic. `TestGraphJS_UnreachableGoal` no longer times out via nil panic; pabt helper constructs identically to production. `grep -R SetAdapter --include=*.go internal/builtin` zero, `grep -R \"NewBridge.*nil\"` zero. `go test ./internal/builtin/bt ./internal/builtin/pabt -race -count=1` both green.",
      "status": "Done",
      "files": [
        "internal/builtin/bt/bridge.go",
        "internal/builtin/bt/require.go",
        "internal/builtin/register.go",
        "internal/builtin/pabt/require_test.go",
        "internal/builtin/bt/bridge_test.go"
      ],
      "contextPointers": {
        "newBridge": "internal/builtin/bt/bridge.go:68-84",
        "pabtHelper": "internal/builtin/pabt/require_test.go:45",
        "btTests": "internal/builtin/bt/bridge_test.go SetAdapter sites"
      }
    },
    {
      "task": "H2 STALL: add IsLoopThread fast path to Bridge.RunSync (mirror TryRunSync) so fork-from-loop no longer 5s/forever freezes (P4)",
      "description": "Bridge.RunSync (bridge.go:357-396) always Submits+blocks, even when caller is loop goroutine; TryRunSync has IsLoopThread fast path (483-498). bt.fork dispatches children on goroutines that tick via TryRunSync->RunSync -> Submit into loop blocked in `<-outputs` parent => 5s stall per wave (raw JS leaves, repro-forkjs 5.001s) or permanent wedge (blocking leaves, repro-fork wedged >=24s). Fix: add `if eventlooputil.IsLoopThread(b.eventLoopGoroutineID.Load()) { return fn(b.vm) }` at RunSync top after started check, identical to TryRunSync. Keep timeout/Done select for off-loop path. Document prohibition: until fork-from-loop tested, script docs warn against `bt.fork` ticked from timer/microtask without this fix.",
      "acceptance": "Fork ticked from loop no longer freezes: repro forkjs completes sub-ms with status success, repro fork no longer wedges (both variant A/B in 09_kill_conditions_verified.md). `TestGraphJS_UnreachableGoal` style fork-with-JS-leaves from both goroutine classes passes. Bridge.RunSync fast path covered by unit test hitting on-loop and off-loop branches; `go test -race ./internal/builtin/bt -run TestBridge_RunSync` green.",
      "status": "Done",
      "files": [
        "internal/builtin/bt/bridge.go",
        "internal/eventlooputil/util.go",
        "internal/builtin/bt/adapter.go"
      ],
      "contextPointers": {
        "runSyncNoFast": "internal/builtin/bt/bridge.go:357-396",
        "tryFast": "bridge.go:483-498",
        "repro": "scratch/engine-slop-autopsy/repro/main.go + evidence/repro-fork*.log"
      }
    },
    {
      "task": "Regression 2b: repair termmux ownership conflict + CaptureSession wait() Promise migration fallout (7 fails -> 0)",
      "description": "Two mechanisms in termmux (evidence/full-internal-tests.log): (a) 4x mouse-drag JS tests build second adapter over already-claimed runtime -> `ownership conflict: runtime already claimed` (new fork enforces one-adapter-per-runtime). Fix tests to reuse provider adapter or create isolated runtimes (like bt tests after fix). Files: module_mouse_drag_test.go:54,74,90,114. (b) 3x CaptureSession tests `wait() result should contain code got \"{}\"`, `isDone false`, `exitCode -1` because wait() changed to `Promise<{code,error}>` but JS assertions read as plain value `{}`. Fix assertions to `await wait()` -> check .code/.exitCode, like WAIT-1 fix siblings (module.go:678-700 passthrough already async). Migrate ~6 cs.wait() sites in module_capture_test.go to loop+await harness via newComplianceEngine. Follow handler already fixed in prior batch but tests not migrated.",
      "acceptance": "All termmux tests green: `go test -run TestHandleMouseDrag_JS -count=1 ./internal/builtin/termmux` no ownership error, `go test -run TestCaptureSession_JSBinding -count=1 ./internal/builtin/termmux` wait() returns Promise<{code,error}> with code 0 and isDone true. `go test ./internal/builtin/termmux -race -count=1` passes (43s -> green). No second adapter construction over claimed runtime remains.",
      "status": "Done",
      "files": [
        "internal/builtin/termmux/module_mouse_drag_test.go",
        "internal/builtin/termmux/module_capture_test.go",
        "internal/builtin/termmux/module.go",
        "internal/builtin/termmux/testhelpers_test.go"
      ],
      "contextPointers": {
        "ownerConflict": "module_mouse_drag_test.go:54 create adapter ownership conflict",
        "waitFallout": "module_capture_test.go:100 wait() result should contain code got {}",
        "compliantPassthrough": "termmux/module.go:678-700 async passthrough pattern"
      }
    },
    {
      "task": "Regression 2c: isolate and repair grpc exported-surface loop-callback failure (6 fails -> 0)",
      "description": "grpc package 6 tests fail inside go-eventloop callback boundary (stack callback.go:474/299) with `TypeError: Cannot read property 'OK' of undefined at <eval>:1:13` and `dial/createClient/createServer/enableReflection/createReflectionClient should be callable false`. All pass at main. Recent bump goja-grpc 39a74e7 + goja-protobuf 9e31b568 changed export surface. Investigate grpc.go Require: registration of `status.OK`, `dial`, `createClient` etc. vs test expects in grpc_test.go:133,164,179,194,209,224. Repro via `go test -run TestStatus_Constants -count=1 ./internal/builtin/grpc -v`. Fix wiring to new adapter surface (e.g., status object nesting, or re-export constants), ensure Bind and loop callback error handling surfaces TypeError correctly, not swallowed. Evidence log trace is starting point.",
      "acceptance": " `go test ./internal/builtin/grpc -count=1 -race` green (6/6 previously failing now PASS), `TestStatus_Constants` can read `status.OK` without TypeError, all `*_Exported` callable checks true. No loop callback swallowed panic; `gmake test -run TestGrpc -count=1` if exists green. Cross-module `go test ./internal/builtin/... -run Grpc` not hanging.",
      "status": "Done",
      "files": [
        "internal/builtin/grpc/grpc.go",
        "internal/builtin/grpc/grpc_test.go",
        "go.mod"
      ],
      "contextPointers": {
        "grpcTests": "internal/builtin/grpc/grpc_test.go:133,164,179,194,209,224",
        "trace": "evidence/full-internal-tests.log:198-248",
        "bump": "go.mod goja-grpc 39a74e7 goja-protobuf 9e31b568"
      }
    },
    {
      "task": "Regression 3 + F2: repair scripting race on Engine.SetGlobal/GetGlobal and legacy globalsMu guard (15 tests poisoned)",
      "description": "engine_core.go:378-387 SetGlobal writes `e.vm.Set(name,value)` under globalsMu from caller goroutine \u2014 mutex protects Go map only, goja Runtime is not goroutine-safe, race detector fires at engine_test.go:422-448 (TestSetGlobal_MutexProtection spawns 100 goroutines mutating VM, asserts safe). Fix: route SetGlobal/GetGlobal through adapter.Submit or Runner.TrySync (like QueueSetGlobal already does at 306/324 via adapter.Submit after prior fix). Make checkEventLoopGoroutine (`threadCheckMode`) default-on or document loop-only with panic, but implementation must be owner-safe. Fix test `TestSetGlobal_MutexProtection` to assert via on-loop serialization, not concurrent VM writes. Verify via `go test -race -count=1 ./internal/scripting -run TestSetGlobal` no race.",
      "acceptance": " `go test -race -count=1 ./internal/scripting -run TestQueueGetGlobal_NilHandling` no race, full `go test -race ./internal/scripting -count=1` 15 downstream fails gone (all green). `grep -n \"vm.Set\\|vm.Get\" internal/scripting/engine_core.go` shows only inside adapter.Submit/loop paths, not bare caller goroutine. Harness integrity (harness.js) unaffected.",
      "status": "Done",
      "files": [
        "internal/scripting/engine_core.go",
        "internal/scripting/engine_test.go",
        "internal/scripting/runtime.go",
        "internal/eventlooputil/util.go"
      ],
      "contextPointers": {
        "setGlobalRace": "internal/scripting/engine_core.go:378-387",
        "testPoison": "engine_test.go:448-453",
        "raceLog": "evidence/race-scripting.txt + evidence/test-scripting-race.log"
      }
    },
    {
      "task": "F3/F4: repair TUIManager.Run main-goroutine VM Get and SwitchMode goroutine discipline",
      "description": "tui_manager.go:752-756 `tm.engine.vm.Get(\"__skipREPL\")` reached via Terminal.Run() on main goroutine concurrent with bubbletea/bt callbacks \u2014 read-only but live window. Fix via `engine.GetGlobal` or `adapter.Submit` read, or cache flag before loop start. SwitchMode (tui_manager.go:300 OnExit, 362 OnEnter, 417+ buildModeCommands) invokes JS callables with no discipline \u2014 production callers happen to be on-loop today via ExecuteCommand Loop.Submit, but tests call from arbitrary goroutines. Fix by routing OnExit/OnEnter through `executeOnLoop`/`adapter.Submit` or asserting IsLoopThread with panic-by-default, and documenting invariant. Add `checkEventLoopGoroutine` guard default-on.",
      "acceptance": "No direct `vm.Get/Set` off-loop in tui_manager.go: `grep -n \"\\.vm\\.Get\\|\\.vm\\.Set\" internal/scripting/tui_manager.go` shows zero outside Submit/RunSync. TUIManager.Run no longer races `__skipREPL` read under `go test -race ./internal/scripting -run TestTUIManager` (or broader scripting race suite). SwitchMode callable invocations are owner-safe; future off-loop caller panics with clear message rather than corrupting.",
      "status": "Not Started",
      "files": [
        "internal/scripting/tui_manager.go",
        "internal/scripting/terminal.go",
        "internal/scripting/engine_core.go",
        "internal/eventlooputil/util.go"
      ],
      "contextPointers": {
        "runRead": "tui_manager.go:752-756",
        "switchMode": "tui_manager.go:300,362,417",
        "executeOnLoop": "engine_core.go:609-639"
      }
    },
    {
      "task": "F5-F8 LOOP MONOPOLIZATION: make termmux passthrough/reader and aimux sync twins and mcpcb initSync async (HIGH)",
      "description": "Freeze-all-JS bindings: termmux switchTo/fromModel.onToggle `s.mgr.Passthrough(s.ctx,cfg)` sync on-loop (module.go:~2080) vs compliant passthrough x2 (1964-1975,678-700) already async via NewPromise \u2014 unify to async Promise. termmux reader() `data,ok:=<-ch` blocking (770-780) -> delete in favor of readAvailable (786-806) non-blocking. aimux sync receive/drainOutput/wait/waitReady (binding_provider.go :184-190,241,251,271) with async twins adjacent -> delete sync variants, keep async, fix drainHandleOutput swallowing non-EOF with continue (292-296) to return error or backoff not hot-spin. mcpcallbackmod initSync/closeSync tempdir+chmod+Listen+file writes sync (mcpcallback.go:707-720,977) -> fold into existing async paths, remove deliberate sync-for-JS-that-cannot-await bypass (violates AGENTS.md). AGENTS.md: no sync waits.",
      "acceptance": "All JS bindings doing I/O return Promise and never block loop: `grep -n \"Passthrough.*s\\.ctx\" internal/builtin/termmux/module.go` shows only async wrappers, `grep -n \"func.*reader()\"` shows no blocking ch recv, `grep -n \"func.*receive\\b\" internal/builtin/aimux` shows only receiveAsync, `grep -n \"initSync\\|closeSync\" internal/builtin/mcpcallbackmod` zero. Manual loop responsiveness test (schedule timer then call passthrough-equivalent) shows timer fires without 5s stall.",
      "status": "Not Started",
      "files": [
        "internal/builtin/termmux/module.go",
        "internal/builtin/aimux/binding_provider.go",
        "internal/builtin/mcpcallbackmod/mcpcallback.go"
      ],
      "contextPointers": {
        "passthroughSync": "termmux/module.go:2080",
        "readerBlock": "module.go:770-780",
        "aimuxSync": "aimux/binding_provider.go:184-190",
        "mcpcbSync": "mcpcallback.go:707-720"
      }
    },
    {
      "task": "F9-F11 SYNC SPAWN/DISK: make exec/aimux/termmux spawns off-loop and termmux/os disk I/O async",
      "description": "Three parallel spawn stacks: exec/exec.go:101 SpawnChild fork/exec on-loop, aimux/binding_provider.go:130-137 cs.Start(ctx) on-loop, termmux/module.go:564-568,971 CaptureSession.start/newBoundedSession sync spawns -> move fork/exec to goroutine via async.Promise with settler, return handle Promise, keep streaming pumps async. termmux sync disk I/O: renderRaster PNG write (1833-1842), saveState/loadState/removeState (2505-2695), restoreState (2634) -> wrap via async.Promise or use Loop.Promisify where *Loop held. os.fileExists sync Stat (os.go:84-104) while siblings readFile/writeFile async -> make fileExists async Promise like siblings (grep fileExists vs readFile). All must propagate ctx and be owner-safe for JS result objects.",
      "acceptance": "`go test -race ./internal/builtin/exec ./internal/builtin/aimux ./internal/builtin/termmux -run TestSpawn -count=1` shows spawn not blocking loop (timer fires during spawn). `gmake test` no longer freezes JS during disk writes. os.fileExists returns Promise<{exists:false}> not sync bool, consistent with os siblings; existing jscompliance modules_pure.spec fileExists pin updated and passes.",
      "status": "Not Started",
      "files": [
        "internal/builtin/exec/exec.go",
        "internal/builtin/aimux/binding_provider.go",
        "internal/builtin/termmux/module.go",
        "internal/builtin/os/os.go"
      ],
      "contextPointers": {
        "spawnStacks": "exec.go:101, aimux:130-137, termmux:564-568",
        "diskIO": "termmux/module.go:1833-1842,2505-2695",
        "fileExists": "os/os.go:84-104"
      }
    },
    {
      "task": "F12-F14 CONTEXT SEVERANCE: thread baseCtx everywhere, delete Background/WithoutCancel fallbacks (7+1+1 sites)",
      "description": "Fix severed cancellation: fetch body stream Background (fetch.go:207-208, now fixed in task 3 but double-check), termmux newBoundedSession context.WithoutCancel(ctx) at 887 silently severs shutdown -> justify or remove (thread base ctx). Defensive Background fallbacks: exec.go:190-192, ctxutil.go:502,518,534, bubbletea.go:341-343, bt/adapter.go:148-149,353-354 (7 sites `if ctx==nil { ctx=Background() }` -> return error or require non-nil). mcpmod/mcp.go:187-196 tool handlers wait on request ctx +30s timer but ignore server base ctx -> include base ctx in select. AGENTS.md: capture base ctx passed to Require and thread into every async goroutine, never Background.",
      "acceptance": "No `context.Background()` in async binding goroutines except legit bootstrap token (runtime.go:115) and bridge childCtx deliberate (bridge.go:107). `grep -R \"context.Background()\" --include=*.go internal/builtin | grep -v \"//\"` shows only whitelisted sites. Shutdown cancels in-flight fetches/streams/sessions/tools: `go test -run TestFetch_Cancel -count=1 ./internal/builtin/fetch -race` shows body read aborts on engine.Close(); termmux session exits on Close.",
      "status": "Not Started",
      "files": [
        "internal/builtin/fetch/fetch.go",
        "internal/builtin/termmux/module.go",
        "internal/builtin/exec/exec.go",
        "internal/builtin/ctxutil/ctxutil.go",
        "internal/builtin/bubbletea/bubbletea.go",
        "internal/builtin/bt/adapter.go",
        "internal/builtin/mcpmod/mcp.go"
      ],
      "contextPointers": {
        "fetchBg": "fetch.go:207-208",
        "withoutCancel": "termmux/module.go:887",
        "bgFallbacks": "exec.go:190, ctxutil 502/518/534, bubbletea 341, bt adapter 148/353",
        "mcpBase": "mcp.go:187-196"
      }
    },
    {
      "task": "F15-F16 SETTLEMENT DISCIPLINE: fix 21 ignored settler errors + empty branch and 3 unchecked Submits",
      "description": "Settler errors ignored at 21 sites across 7 files (exec.go:32,41; fetch.go:58,78,84,89,92; mcp.go:346,348; mcpcallback.go:307,316,354,557,773,809,855; termmux/module.go:623,699,1973; tokenizer.go:94; async/promise.go:25,30). Only async/promise.go:35 inspects ErrAdapterInvalid/ErrPromiseSettled but body is empty comment, Reject ignores unconditionally. Fix: make helper handle both symmetrically \u2014 log at debug or return handled error, document tolerance. Then make async.Promise sole construction site: delete hand-rolled NewPromise+goroutine+`_ =` in 7 modules, migrate to helper, ensure every settler Resolve/Reject error checked (not `_ =`). Also fix 3 unchecked fire-and-forget Submit (termmux/module.go:1057, mux_state.go:66, mcpcallback.go:903) to check error and log on shutdown drop, aligning with checked sites 353,843,878.",
      "acceptance": "`grep -R \"_ = settler\" --include=*.go internal/builtin` shows zero; `grep -R \"_ =.*Submit\" --include=*.go internal/builtin` shows zero unchecked (all checked or explicitly log). async/promise.go no empty branch \u2014 either logs or handles ErrPromiseSettled/ErrAdapterInvalid symmetrically for Resolve/Reject. `go vet` zero; `go test -race ./internal/builtin/... -count=1` shows no dropped settler errors under shutdown race.",
      "status": "Not Started",
      "files": [
        "internal/builtin/async/promise.go",
        "internal/builtin/exec/exec.go",
        "internal/builtin/fetch/fetch.go",
        "internal/builtin/mcpmod/mcp.go",
        "internal/builtin/mcpcallbackmod/mcpcallback.go",
        "internal/builtin/termmux/module.go",
        "internal/builtin/termmux/mux_state.go"
      ],
      "contextPointers": {
        "ignoredSites": "exec.go:32,41 fetch.go:58 etc mux_state.go:66",
        "emptyBranch": "async/promise.go:35-38",
        "uncheckedSubmit": "module.go:1057 mux_state.go:66 mcpcallback.go:903"
      }
    },
    {
      "task": "Collapse wait-for-loop sextet + lifecycle triplets into one Runner substrate (largest simplification, ~350 lines deleted)",
      "description": "Six wait-for-loop impls differ accidentally: Runtime.RunSync (304-342,5s), Runtime.TryRunSync (347-361, fast), Bridge.RunSync (357-396,5s no fast), Bridge.TryRunSync (483-498 fast), Engine.executeOnLoop (609-639 no timeout), TUIManager.scheduleWriteAndWait (198-228), plus bootstrap one-off (bridge.go:134-148). Also lifecycle triplets (Runtime/Bridge/Engine started/stopped+IsRunning+Done) and 3x goroutineId captures (runtime 148-150, bridge 178, engine 215 mirroring). Collapse into `internal/eventlooputil.Runner{Loop,VM,OwnerID,Done,Timeout}` with Go/Sync/TrySync handling timeout+Done+fast path once. Runtime/Bridge/Engine/TUIManager delegate; Engine drops mirrored goroutineId field, delegating to runtime.GoroutineID(). H2 fix becomes unrepresentable because one place holds fast-path invariant. Delete bootstrap seventh variant by reusing Runner.",
      "acceptance": "One Runner implementation exists in internal/eventlooputil/runner.go with Go/Sync/TrySync and tests; Runtime/Bridge/Engine no longer have duplicate submit+select+timer code. `grep -n \"errCh := make(chan error\" --include=*.go internal/` shows only Runner; `wc -l` net deletion >=300 lines across 6 files. `go test -race ./internal/scripting ./internal/builtin/bt ./internal/builtin/pabt -count=1 -run Test.*Sync` green; fork-from-loop repro still sub-ms; Bridge missing fast path class eliminated structurallly.",
      "status": "Not Started",
      "files": [
        "internal/eventlooputil/runner.go",
        "internal/eventlooputil/util.go",
        "internal/scripting/runtime.go",
        "internal/builtin/bt/bridge.go",
        "internal/scripting/engine_core.go",
        "internal/scripting/tui_manager.go"
      ],
      "contextPointers": {
        "sextet": "runtime.go:304,347 bridge.go:357,483 engine_core.go:609 tui_manager.go:198",
        "triplets": "runtime.go:49-52 bridge.go:46-48 engine_core.go:86",
        "goroutineId3": "runtime 148 bridge 178 engine 215"
      }
    },
    {
      "task": "Unify bt seam: callLeaf, composite loader, ticker/manager wrapper, per-ticker liveness fold (4.3-4.7)",
      "description": "Leaf scaffolding x4 (jsHelpers.runLeaf, JSLeafAdapter.dispatchJSWithGen, BlockingJSLeaf on/off-loop) are near-verbatim wrapperFn+runLeaf+parse status+callback. Extract `callLeaf(vm,tickFn,ctxVal,onResult)` shared. Composite triplets sequence/fallback/selector (require.go:84-159, fallback===selector) -> parameterize by `bt.Selector` vs `bt.Sequence` to ~20 lines. Ticker/manager wrappers createTickerJSWrapper/createManagerJSWrapper duplicate done/err/stop/_native + Once+Promise -> generic `createHandleJSWrapper[T DoneErrStop]` over interface. Fold per-ticker double goroutine (Promisify liveness token + done Promise waiting same Done) into single registered worker holding both. Rewrite jsHelpers/unwrap comments against actual fork semantics (microtask queue exists) and delete CRIT-x/review-doc archaeology.",
      "acceptance": "Net deletion >=80+55+60 lines; `grep -n \"createLeaf\\|callLeaf\" internal/builtin/bt` shows one callLeaf. `grep -n \"fallback\\|selector\" internal/builtin/bt/require.go` shows one loader factory. Single ticker wrapper covers both. Per-ticker goroutines halved (1 worker vs 2, plus done Promise worker folded). `go test -race ./internal/builtin/bt -count=5` stable; `go test -run TestBT_SequenceFallbackSelector` still covers composites via parameterized loader.",
      "status": "Not Started",
      "files": [
        "internal/builtin/bt/adapter.go",
        "internal/builtin/bt/require.go",
        "internal/builtin/bt/bridge.go",
        "internal/builtin/bt/unwrap.go",
        "internal/builtin/bt/doc.go"
      ],
      "contextPointers": {
        "leaf4": "adapter.go:262-303,368-427,450-505 bridge.go:188-225",
        "composite": "require.go:84-159",
        "ticker2": "require.go:327-330+351-358",
        "staleMicro": "bridge.go:192-195 unwrap.go:136-139 doc.go:30"
      }
    },
    {
      "task": "Eliminate dead params/code, state-refresh storm, stale comment layer (4.6,4.8,4.9)",
      "description": "Delete `_ any` corpse params NewReadableStream/wrapReadableStreamJS (readable_stream.go:37,138) already done in task 3 but verify plus template.Require+unicodetext.Require ctx params never used (remove). Fix async/promise.go:35-38 empty error branch (handled in task 13). Delete Bridge.RunJSSync alias (401-403) move to bubbletea.JSRunner consumer side if needed. Delete BlockingJSLeaf off-loop defer draining channel into nothing (adapter.go:441-448). Fix state-refresh storm engine_core.go:267-270 `go bubbleteaMgr.SendStateRefresh(key)` per SetState -> coalescing latest-wins dispatcher (serialized per key, 18 lines, no stale-key ordering). Rewrite stale no-microtask comments (bridge.go:192-195, unwrap.go:136-139, doc.go:30) to describe Node-v11+ alternating batches + Adapter.Bind microtask queue, and delete 5 nonexistent review-doc citations (bridge.go:88, adapter.go:53,92,191, engine_core.go C5x4) and spec punt `loadFile is async, covered by contract` (modules_pure.spec.js:116).",
      "acceptance": " `grep -n \"_ any\" internal/builtin/fetch/readable_stream.go` zero, `grep -n \"Require.*baseCtx\" internal/builtin/template` zero, `grep -n \"RunJSSync\" internal/builtin/bt/bridge.go` zero (alias gone), `grep -n \"drain.*channel\" internal/builtin/bt/adapter.go` zero dead defer. Rapid churn test (100 SetState in loop) spawns bounded goroutines and delivers latest key refresh deterministically. `grep -R \"no microtask\" --include=*.go --include=*.md` shows zero false claims; `grep -R \"review-1.md\\|CRIT-\"` zero. `go vet` clean.",
      "status": "Not Started",
      "files": [
        "internal/builtin/fetch/readable_stream.go",
        "internal/builtin/template/template.go",
        "internal/builtin/unicodetext/unicodetext.go",
        "internal/builtin/bt/bridge.go",
        "internal/builtin/bt/adapter.go",
        "internal/scripting/engine_core.go",
        "internal/builtin/bt/unwrap.go",
        "internal/jscompliance/specs/modules_pure.spec.js"
      ],
      "contextPointers": {
        "deadAny": "readable_stream.go:37,138",
        "storm": "engine_core.go:267-270",
        "stale": "bridge.go:192-195 unwrap.go:136-139"
      }
    },
    {
      "task": "Adopt unused fork surface: WithLogger/Loop.Log + WithMetrics + WithDebugMode for observability (10 top leverage)",
      "description": "Wire the two highest-leverage unused capabilities so regressions become loud: (a) runtime.go loop construction `goeventloop.New(WithAutoExit(true), WithLogger(slog bridge), WithMetrics(true), WithDebugMode(env DEBUG))` plus `SetConsoleOutput` handling \u2014 panics in callbacks log via Loop.Log isolated goroutine instead of silent hang, promise creation stacks and unhandledRejection reports answer 'where did this promise come from' (P1/P4 panics would have logged `async: adapter is nil`). (b) Optionally expose metrics via debug flag or `osm:debug` module (callback latency percentiles, queue depths) to quantify perf mandate instead of asserting. Document deliberately unused RegisterFD (epoll/kqueue) as channel-based is legitimate for Windows ErrReadinessUnsupported \u2014 add comment in tui_manager.go/termmux. Evidence for decision in scratch/engine-slop-autopsy/10_unused_surface.md.",
      "acceptance": "WithLogger wired: a deliberately panicking binding (test helper) emits structured log via Loop.Log and `go test -run TestPanicLog -count=1` captures it; previously silent 5s timeout now shows `LOG: async: adapter is nil` before timeout. WithMetrics enabled, `Loop.Metrics()` callback latency percentiles available via debug env. `grep -R \"WithLogger\\|WithMetrics\\|Loop.Log\" --include=*.go internal/scripting` shows usage at runtime.go:86-88. Unused RegisterFD has rationale comment, not silent omission.",
      "status": "Not Started",
      "files": [
        "internal/scripting/runtime.go",
        "internal/builtin/register.go",
        "internal/eventlooputil/runner.go",
        "internal/scripting/tui_manager.go"
      ],
      "contextPointers": {
        "unusedDoc": "scratch/engine-slop-autopsy/10_unused_surface.md:14-24",
        "loopLog": "goeventloop-doc.log *Log* WithLogger",
        "metrics": "goeventloop doc WithMetrics Loop.Metrics"
      }
    },
    {
      "task": "Verification parity: harden async helper, tokenizer, bt fork, fetch suite (makes Phase 0-3 safe, closes H10)",
      "description": "Add tests that would have caught all regressions: (a) internal/builtin/async: nil adapter handling, double-settle (ErrPromiseSettled), shutdown race (adapter.Done closed), panic-in-fn propagation (decide recover+reject). Currently zero test files \u2014 add async_test.go. (b) tokenizer first-ever tests: loadFile/loadJSON success+error+race hammer. (c) bt: fork-with-JS-leaves from both goroutine classes (loop vs non-loop), two tickers sharing one raw JS node (H3 refuted but still regression test), orphan ticker liveness pin (Bridge.Stop reaps). (d) fetch: restore body-streaming suite to main tier and ensure 4 tests are in `go test ./...` path, not compliance-only. All run with -race -count=1.",
      "acceptance": " `go test -race -count=1 ./internal/builtin/async` passes with 4+ subtests; `./internal/builtin/tokenizer -race` passes with hammer; `./internal/builtin/bt -race -run TestFork` passes sub-ms for both variants (forkjs/fork). Fetch suite 4 tests green without -run filter. `grep -R \"no test files\" internal/builtin/async` zero \u2014 helper now has coverage.",
      "status": "Not Started",
      "files": [
        "internal/builtin/async/async_test.go",
        "internal/builtin/tokenizer/tokenizer_test.go",
        "internal/builtin/bt/fork_test.go",
        "internal/builtin/fetch/fetch_test.go",
        "internal/builtin/fetch/readable_stream_test.go"
      ],
      "contextPointers": {
        "asyncZero": "evidence/REPORT.md async [no test files]",
        "tokenizerZero": "tokenizer [no test files] modules_pure.spec.js:116",
        "forkK1": "09_kill_conditions_verified.md K1"
      }
    },
    {
      "task": "Future-proof hardening: add -race tier, fuzz, determinism, coverage, verification blind spot closure (H10 permanent)",
      "description": "Close pipeline blind spots permanently: add `test-engine` target to config.mk: `go test -race -count=1 -timeout=300s ./internal/scripting ./internal/builtin/...` and make it prerequisite for `test` tier. Add fuzz targets for jscompliance harness and context parsers (fuzz_test.go, 30s) and ensure `gmake fuzz` no crashers. Run full suite `-race -count=5` and verify determinism (same pass rate, no flake). Measure coverage via `gmake cover` and ensure >80% on compliance-critical paths (harness.js, module_hardening.go, Runner). Verify no host mutation (t.TempDir/Setenv) and no build tags. Document why compliance targets alone cannot catch binding regressions (config.mk:81-102).",
      "acceptance": " `gmake test-engine` exists in config.mk and runs `-race ./internal/...` (excluding scratch). `gmake fuzz` 30s per target shows no crashers. `go test -race -count=5 ./internal/scripting ./internal/builtin/bt -count=1` deterministic (same quantified pass rate). `gmake cover` reports >80% on internal/scripting+async. `grep -R \"t.TempDir\" --include=*_test.go | wc -l` vs host mutation zero; `grep -R \"build tags\"` zero via standard patterns only. Blind-spot class can no longer ship silently.",
      "status": "Not Started",
      "files": [
        "config.mk",
        "internal/jscompliance/fuzz_test.go",
        "internal/builtin/ctxutil/ctxutil_fuzz_test.go",
        "Makefile"
      ],
      "contextPointers": {
        "blindSpot": "05_gap_analysis.md H10, evidence/config.mk:81-102",
        "preCoverage": "gmake cover, go test -cover"
      }
    },
    {
      "task": "Adapt charm/lipgloss/bubbles + go-git alpha.5 bumps and align docs/AGENTS (drift closure)",
      "description": "Dependency bump at cd0578a: bubbles 2.2.1, go-eventloop 22f9a6a, go-inprocgrpc e327357, goja-grpc 39a74e7, goja-protobuf 9e31b568. Audit each changelog: bubbletea 2.0.9 key pipeline, lipgloss width/height, bubblezone hit-testing, go-billy alpha.2 securejoin removal, charm x/term. Fix internal/builtin/bubbletea, lipgloss, termui, gitops. Align docs: AGENTS.md Concurrency/Binding Contract already correct but re-verify after Runner collapse; docs/scripting.md module tables describe Promise async returns and strict always-on; docs/architecture.md diagrams show Adapter.Submit/NewPromise not Loop(); docs/reference/js-compliance-suite.md remove WithStrictMicrotaskOrdering mention; TestModuleSurface zero undocumented after drift fix. `gmake test ./internal/builtin/bubbletea ./internal/builtin/lipgloss ./internal/builtin/gitops ./internal/termui -race` green.",
      "acceptance": "Charm bindings render identically (existing bubbletea/lipgloss tests + key-input fixtures green, no deprecated API warnings). `gmake vet` clean, `GOOS=linux/windows go build ./...` green. Docs grep `WithStrictMicrotaskOrdering` zero except CHANGELOG; architecture diagrams consistent with new Runner. TestModuleSurface logs zero undocumented exports.",
      "status": "Not Started",
      "files": [
        "internal/builtin/bubbletea/bubbletea.go",
        "internal/builtin/lipgloss/lipgloss.go",
        "internal/builtin/gitops/module.go",
        "internal/termui/**",
        "docs/scripting.md",
        "docs/architecture.md",
        "docs/reference/js-compliance-suite.md",
        "AGENTS.md"
      ],
      "contextPointers": {
        "bump": "go.mod 20260824 patches",
        "driftTags": "DRIFT-7/8/10 docs/scripting.md#Native modules"
      }
    },
    {
      "task": "DUAL TARGET MANDATE: make BOTH `gmake -j 10 make-all-with-log` and `gmake -j 10 make-all-in-container` pass \u2014 update container Go version pin if needed",
      "description": "ContinuousVerification dual mandate: run `mkdir -p scratch && gmake -j 10 make-all-with-log 2>&1 | tee scratch/make-all-with-log.log | tail -n 200; echo EXIT:$PIPESTATUS` \u2014 must show `ok` for all packages, no FAIL, vet/staticcheck/deadcode zero. Then run `gmake -j 10 make-all-in-container 2>&1 | tee scratch/make-all-in-container.log | tail -n 200`. Container derives `golang:$go_version` from `go mod edit -print | awk '/^go / {print $2}'` (1.27.0). Verify image exists via `docker manifest inspect golang:1.27.0` or pull. If absent (common for very new Go), update config.mk and example.config.mk: fallback to `golang:1.27`, `golang:1.27-bookworm`, or latest patch, stripping patch version `${go_version%.*}` or pinning `1.27.1` etc., and document. Fix any Linux-specific failures surfaced (cgo, pty, file paths). Both logs must end EXIT:0. This task is the commit gate for the whole blueprint.",
      "acceptance": " `scratch/make-all-with-log.log` shows `ok` for all 83+ packages, FAIL count zero, and `build.log` tail shows zero FAIL lines after `go test -timeout=20m ./...`. `scratch/make-all-in-container.log` likewise shows `ok` inside docker golang image (same counts, linux). `docker run --rm golang:$resolvedVer go version` matches 1.27.x. If image fallback used, `example.config.mk` updated and committed with comment. Neither target shows `operation timed out` or `SANDBOX_BREACH`.",
      "status": "Not Started",
      "files": [
        "config.mk",
        "example.config.mk",
        "scratch/make-all-with-log.log",
        "scratch/make-all-in-container.log",
        "build.log",
        "Makefile"
      ],
      "contextPointers": {
        "makeAll": "Makefile:400-414 all -> _all__build/lint/test",
        "containerDerive": "config.mk:26 go_version awk",
        "buildLog": "build.log 392 lines"
      }
    },
    {
      "task": "Final Rule of Two strict-review-gate across whole blueprint before mark Done (adversarial, 2x contiguous)",
      "description": "After dual-target green, invoke /strict-review-gate twice contiguously with hostile reviewers. Reviewers check: (1) sandbox closed not shimmed, (2) tokenizer race gone via settler, (3) fetch streaming+liveness correct, (4) Bridge construction invariant, (5) Bridge fast path, (6) termmux owner/wait, (7) grpc loop callback, (8) scripting race, (9) SwitchMode discipline, (10) loop monopolization deleted, (11) spawn/disk async, (12) context severance, (13) settlement discipline, (14) Runner collapse, (15) bt seam, (16) dead code/comments, (17) WithLogger/Metrics wired, (18) verification parity, (19) H10 tier, (20) charm/docs, (21) dual container target. Any finding resets pair. Also run `gmake vet && gmake build && GOOS=linux/windows go build ./...` and `grep -R legacy` zero. Only after two clean passes may blueprint be marked Done and committed.",
      "acceptance": "Two contiguous subagent reviews confirm zero unresolved findings, with logs in scratch/review-*.log showing hostile checks passed. `gmake -j 10 make-all-with-log` and `make-all-in-container` still green after reviews. Blueprint statusSection.currentState updated to Done, commit message references Replan 7 and lists fixed regressions. No hard-coded test-only special casing detected.",
      "status": "Not Started",
      "files": [],
      "contextPointers": {
        "dependsOn": "all prior tasks 1-21 green"
      }
    }
  ],
  "continuousVerification": {
    "task": "Keep both make targets green after EVERY change; no silent hangs",
    "description": "After each code change: (a) legacy grep zero, (b) `go test -race -count=1 -timeout=20m ./internal -run TestSandbox` green, (c) `go test -race -count=1 ./internal/builtin/fetch -run TestResponseBody` green, (d) `go test -race -count=1 ./internal/builtin/pabt -run TestGraphJS` green, (e) `go test -race -count=1 ./internal/builtin/termmux -run TestHandleMouseDrag|TestCaptureSession` green, (f) `go test -race -count=1 ./internal/builtin/grpc -run TestStatus` green, (g) `gmake -j 10 make-all-with-log` tail shows zero FAIL (or per-task focused `go test` subset), (h) periodically `gmake -j 10 make-all-in-container` to catch Linux-only. Every `tail` must have been `tee scratch/` first.",
    "failPolicy": "If ANY unintended FAIL or grep hit or 5s/10s timeout appears, STOP and fix \u2014 zero tolerance for hangs, races, or flakiness. Intended fails: none \u2014 all 22 tasks must make previously red tests green. Fork-blocked quant tier remains separate but main `go test ./...` must be fully green.",
    "testCoverage": "Coverage must span sandbox, tokenizer race hammer, fetch streaming liveness, Bridge fast path (on/off loop), termmux owner, grpc export, scripting race, monopolization deletions, Runner, bt seam, observability log, and dual container target."
  },
  "finalEnforcementProtocol": [
    {
      "type": "POST_EXECUTION_WARNING",
      "alert": "The 20260823/24 forks are not upstream. Adapter.NewPromise+Settler and Adapter.Submit are the ONLY async paths \u2014 never reintroduce JS.NewChainedPromise or Loop exposure via shims. Delete boldly per Internal API Discipline."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "Strict microtask ordering is always-on. Do NOT re-add WithStrictMicrotaskOrdering. Verify via core_microtask.spec.js, not option plumbing."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "Both make targets are MANDATORY: `gmake -j 10 make-all-with-log` local and `gmake -j 10 make-all-in-container` docker (golang:1.27.0 or fallback in example.config.mk) must both be green with zero FAIL \u2014 this repeats the globalAlerts dual-target warning for reinforcement."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "Quantified language compliance is closed \u2014 not the tail. Binding-layer correctness and dual-target green are the tail. Harness integrity (__activeSink) plus binding tests are load-bearing."
    }
  ]
}
