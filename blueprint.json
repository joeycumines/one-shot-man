{
  "meta": {
    "fileName": "blueprint.json",
    "maintenanceInstruction": "KEEP THE PLAN UP TO DATE AS YOU EVOLVE THE CODE. DEVIATIONS TO THE PLAN MUST BE LOGGED WITHIN THE PLAN. THE PLAN MUST BE REASSESSED HOLISTICALLY AFTER ANY CHANGE OF ANY SIZE.",
    "completionPolicy": "YOU ARE NOT DONE UNTIL YOU HAVE COMPLETED 100% OF THE BLUEPRINT. DO NOT STOP. YOU MUST REFINE AT REGULAR INTERVALS TO KEEP THE BLUEPRINT FULL. NEVER SPECIFY ANY ESTIMATE OF EFFORT OR COST — ESPECIALLY NOT TIME ESTIMATES. NEVER SPECIFY A PRIORITY OF ANY KIND."
  },
  "globalAlerts": [
    {
      "warning": "ALL checks must pass: `make` runs build, lint (vet, staticcheck, betteralign, deadcode), and test with race detection. NEVER commit with failing checks. Pipe long output through `tail -200` to avoid context window exhaustion."
    },
    {
      "warning": "Rule of Two is MANDATORY before every commit: two contiguous, issue-free subagent reviews of the exact same diff. Context-contamination resets to 0. Use strict-review-gate skill protocol EXACTLY."
    },
    {
      "warning": "Each task follows: work → check (make) → Rule of Two (strict-review-gate) → Commit (git-commit skill). Cycles are mandatory on failure until task is ACTUALLY COMPLETE AND CORRECT."
    },
    {
      "warning": "JS runs in Goja event loop — ALL blocking operations freeze the BubbleTea UI. Never call synchronous exec.execv() in update() or in async Promise continuations that run on the Goja microtask queue. Use exec.spawn() + await for all subprocess calls, or tea.tick() to yield between steps."
    },
    {
      "warning": "super_document_script.js is the REFERENCE implementation for viewport/scrollbar/mouse/keyboard patterns. ALL TUI code must follow its patterns exactly."
    },
    {
      "warning": "internal/termmux must remain clean of pr-split-specific logic. It will be extracted to its own module. Only general-purpose terminal multiplexer functionality belongs there."
    },
    {
      "warning": "The Mux VTerm captures ALL child output via teeLoop. Mux.ChildExitOutput() → vtm.String() (plain text). Mux.ChildScreen() → vtm.RenderFullScreen() (ANSI). CaptureSession has both output() and screen() (ANSI). Always use ANSI methods for visual rendering and plain text methods for assertions/diagnostics."
    },
    {
      "warning": "exec.spawn() returns a Promise-based ChildHandle with stdout/stderr ReadableStreams and await-able wait(). This is the REQUIRED interface for non-blocking subprocess operations in async JS contexts. exec.execv() is ONLY acceptable in REPL mode or pre-BubbleTea contexts."
    }
  ],
  "mandatoryDirectives": {
    "contextManagement": "Tasks are sequential. Complete one fully (including Rule of Two + commit) before starting the next. Each task is self-contained. Break work into manageable units.",
    "claudeIntegrationMandate": "The core value proposition of osm pr-split is DEEP Claude integration with an interactive, muxed terminal pane embedded in the TUI. Every wizard phase must surface Claude's capabilities: prompting, classification, plan review feedback, error diagnosis, conflict resolution. The Claude pane must be ANSI-rendered, accept keyboard input when focused, and be switchable between screenshot mode and full passthrough.",
    "eventLoopMandate": "The Goja event loop processes microtasks (Promise continuations) after each _updateFn call. ANY blocking call in a Promise continuation freezes the UI until the call returns. All subprocess calls in async contexts MUST use exec.spawn() + await. Heuristic tick handlers MUST yield between operations via tea.tick()."
  },
  "statusSection": {
    "goalState": "A fully functional, production-quality pr-split TUI wizard with: (1) ANSI-rendered interactive Claude terminal pane with keyboard forwarding, (2) non-blocking async operations via exec.spawn throughout, (3) proper viewport/scrollbar on all screens, (4) working keyboard+mouse navigation matching super-document patterns, (5) real Claude integration with back-and-forth interaction in EVERY wizard phase, (6) embedded terminal output as BubbleTea component via termmux, (7) proper signal handling, (8) working error resolution flow with Claude diagnostics, (9) post-split verification with live CaptureSession output, (10) correct colors/layout with WCAG AA compliance, (11) comprehensive test coverage including real-Claude integration tests and VTerm observation tests, (12) exhaustive documentation of flows, architecture, and interactive patterns.",
    "completedWork": [
      "T01: Real-Claude integration tests (classification accuracy, plan quality, MCP round-trip, fallback, conflict resolution)",
      "T02: Mock MCP pipeline tests (schema validation, error recovery, edge cases)",
      "T03: View Report overlay (scrollable viewport, styled, clipboard)",
      "T04: Plan editor modal dialogs (move-file, rename-split, merge-splits)",
      "T05: output.print audit (zero BubbleTea-reachable calls)",
      "T06: Keyboard focus indicators (focusIndex, per-screen elements, visual styles)",
      "T07: Responsive layout breakpoints (compact/standard/wide, side-by-side plan review)",
      "T08: OS-level signal handling (SIGINT/SIGTERM, terminal restore, double-SIGINT force-exit)",
      "T09: Clean exit audit (all 8 exit paths, deferred terminal finalizer)",
      "T10: Strategy validation UI (Claude availability check, auto-fallback)",
      "T11: Post-split verification display (per-branch status, expandable output, equiv check)",
      "T12: CaptureSession in termmux (PTY + VTerm, Start/IsRunning/Output/Screen/Write/Kill)",
      "T13: CaptureSession JS bindings (newCaptureSession factory, full API)",
      "T14: CaptureSession integration in pr-split TUI (live verification viewport, interrupt)",
      "T15: Claude window-in-window split-view (Ctrl+L toggle, screenshot poll, adjustable ratio)",
      "T16: Interactive Claude back-and-forth (inline text input, Ask Claude, history)",
      "T17: Plan editor inline editing (title edit, file checkboxes, reordering, chunk nav)",
      "T18: WCAG AA color audit (all pairs pass, dark+light terminals)",
      "T19: Comprehensive view renderer tests (all 10 views, zone marks, extreme sizes)",
      "T20: Keyboard and mouse event handling tests (Tab cycling, arrow nav, all zones)",
      "T21: TUI async pipeline integration tests (7+ flow paths, error recovery, cancel)",
      "T22: VTerm-based TUI rendering tests (config, analysis, plan, keyboard, mouse)",
      "T23: Mouse zone coverage audit (34 zone IDs, all handlers, wheel guard fix)",
      "T24: Keyboard routing audit (6 overlay interceptors, arrows context-aware, help updated)",
      "T25: Claude crash detection and recovery (5s health poll, 60s heartbeat, restart/fallback/abort)",
      "T26: REPL command unit tests (35 test functions, 90+ subtests, all 32 commands)",
      "T27: Mux.ChildScreen() ANSI + Mux.WriteToChild() input — termmux extensions with JS bindings and 11 tests",
      "T28: Claude split-view ANSI rendering — RenderContentANSI, ContentANSI, ChildScreen ANSI, renderClaudePane ANSI-aware, fallback to plain",
      "T29: Claude pane keyboard input forwarding — keyToTermBytes, CLAUDE_RESERVED_KEYS, writeToChild, INPUT badge, scroll keys before PTY",
      "T30: gitExecAsync() non-blocking git helper — exec.spawn with parallel stream collection, same {stdout,stderr,code} shape, export",
      "T31: Convert automatedSplit pipeline to gitExecAsync — 8 async functions, 7 strategy fix() methods, heuristicFallback/resolveConflictsWithClaude, late-binding, 13 test files updated",
      "T61: mcpCallback.waitForAsync() — non-blocking Promise-based MCP tool wait with aliveCheck trampoline, 9 tests, 47/47 pass",
      "T62: Pipeline async conversion — waitForLogged dual-path, step callbacks async, spawn/restart async, handleRestartClaudePoll, scroll keys, 485 tests pass"
    ],
    "replanLog": [
      {
        "trigger": "Session restart with full code exploration. Many old 'Not Started' tasks already have substantial implementation. Blueprint rewritten from scratch to reflect actual codebase state.",
        "affectedTasks": "All tasks rewritten. Old T01-T05+TSplit pruned (Done). Old T06-T25 replaced with 33 refined tasks. Integration tests locked at T01. Dependencies corrected.",
        "changes": "Flat sequential rewrite. No priorities. No estimates. Integration tests T01 position locked. Function-name anchors replace line numbers."
      },
      {
        "trigger": "Comprehensive architectural analysis uncovered critical deficiencies: (1) Claude pane is read-only plain-text, needs ANSI+input, (2) 85+ blocking exec.execv calls freeze event loop during Promise continuations, (3) 'Test Connection' only checks binary existence, (4) No integrated Claude flows per wizard phase, (5) Old T27-T33 insufficient scope. Full rewrite of pending tasks.",
        "affectedTasks": "T27-T33 replaced with T27-T60 covering: termmux ANSI+write extensions, event loop non-blocking conversion, Claude deep integration, UI fixes, comprehensive testing, documentation rewrite, cross-platform validation.",
        "changes": "34 new tasks replacing 7. Old T27→T49, T28→T50, T29→T51, T30→T52 (expanded), T31→T53, T32→T54, T33→T55. 27 entirely new tasks for Claude integration, event loop fixes, and comprehensive testing."
      },
      {
        "trigger": "Deep analysis of mcpcallbackmod/mcpcallback.go revealed jsWaitFor() blocks the Goja event loop goroutine for up to 5-30 minutes via for/select loop. This is the SINGLE MOST CRITICAL blocking bug. Also identified missing CHANGELOG task.",
        "affectedTasks": "T61 (waitForAsync), T62 (pipeline conversion), T63 (CHANGELOG) added. T31 dependencies updated: T31 now also depends on T62 (pipeline must use waitForAsync). Execution order: T27→T28→T29→T30→T61→T62→T31→T32→T33→T34→T35→...→T63.",
        "changes": "3 new tasks (T61-T63). T31 dependency chain corrected. Total: 63 tasks (26 Done, 37 Not Started)."
      }
    ]
  },
  "sequentialTasks": [






    {
      "task": "T32: Convert heuristic-path runAnalysisStep to async Promise+poll pattern",
      "description": "runAnalysisStep(s, stepIdx) is called from BubbleTea tick handlers and directly calls analyzeDiff() → gitExec() → blocks. Convert to Promise+poll pattern matching the automatedSplit approach. Changes: (a) Create startAnalysisAsync(s) that launches analyzeDiff as a Promise (using the async version from T31), stores the Promise in s.analysisPending, and starts polling via tea.tick(100, 'analysis-poll'). (b) Create handleAnalysisPoll(s) that checks if s.analysisPending has resolved: if no, continue polling; if yes, process result and advance to next step. (c) Each analysis step (0-3) becomes a Promise: step 0=analyzeDiff, step 1=applyStrategy, step 2=createSplitPlan, step 3=validatePlan. Each resolves into the next via chaining. (d) Update _updateFn to replace analysis-step-N ticks with analysis-poll. (e) Remove the old runAnalysisStep function. (f) Verify: during heuristic analysis, the UI remains responsive, spinner/progress updates, cancel works immediately.",
      "acceptance": "Heuristic analysis path is fully non-blocking. Progress spinner updates during analysis. Cancel via Ctrl+C processes within 1 second during any analysis step. View renders correctly between steps. All analysis tests pass.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (runAnalysisStep — search for analysis-step, line ~700)"
        ],
        "references": [
          "internal/command/pr_split_16_tui_core.js (startAutoAnalysis — Promise+poll pattern reference at line ~2049)"
        ],
        "dependencies": [
          "T31"
        ]
      }
    },
    {
      "task": "T33: Convert heuristic-path runExecutionStep to per-branch yielding",
      "description": "runExecutionStep(s, 0) calls prSplit.executeSplit() which creates ALL branches sequentially via blocking gitExec() calls. This blocks for total_branches × branch_creation_time. Convert to per-branch yielding. Changes: (a) Create executeSplitAsync(plan, config) async function that yields between branches: for each split, await gitExecAsync for checkout/apply/commit, then yield via await new Promise(resolve => setTimeout(resolve, 1)). (b) Launch as Promise in startExecutionAsync(s) (same Promise+poll pattern as T32). (c) Report per-branch progress to model state: s.executionBranchIdx, s.executionBranchTotal, s.executionCurrentBranch. (d) Update viewExecutionScreen to show per-branch progress during creation (not just after). (e) Polling checks s.executionRunning flag every 100ms. (f) Cancel aborts mid-branch and cleans up partial branches.",
      "acceptance": "Branch creation shows per-branch progress in real-time. UI responsive during execution. Cancel mid-creation cleans up partial branches. All existing execution tests pass. Performance: no worse than 10% overhead vs blocking path.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (runExecutionStep — search for exec-step, line ~640)",
          "internal/command/pr_split_05_execution.js (executeSplit)"
        ],
        "references": [
          "internal/command/pr_split_16_tui_core.js (startAutoAnalysis — Promise+poll pattern)"
        ],
        "dependencies": [
          "T31"
        ]
      }
    },
    {
      "task": "T34: Convert verifyEquivalence to non-blocking via CaptureSession",
      "description": "verifyEquivalence() in pr_split_06_verification.js runs multiple blocking git commands (merge-base, diff-tree, tree comparisons). Convert to non-blocking. Changes: (a) Create verifyEquivalenceAsync() that uses gitExecAsync for all git operations. (b) Launch as Promise in the execution pipeline (polled with tick). (c) Report progress: s.equivCheckStatus = 'running' | 'pass' | 'fail'. (d) Show spinner/progress during equivalence check. (e) If equivalence check involves large diffs, stream output via CaptureSession for live display. (f) Update viewVerificationScreen to show progress during check.",
      "acceptance": "Equivalence check runs without blocking UI. Progress indicator visible. Cancel works. Result displayed correctly (pass/fail with tree hashes). All existing verification tests pass.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_06_verification.js (verifyEquivalence — search for verifyEquivalence)",
          "internal/command/pr_split_16_tui_core.js (exec-step-2 handler)"
        ],
        "references": [
          "internal/command/pr_split_00_core.js (gitExecAsync from T30)"
        ],
        "dependencies": [
          "T30"
        ]
      }
    },
    {
      "task": "T35: Fix 'Test Connection' to actually test Claude MCP round-trip",
      "description": "Current 'Test Connection' (handleClaudeCheck in pr_split_16_tui_core.js) only checks if Claude binary exists via executor.resolve(). This is useless — it doesn't verify Claude can actually communicate. Fix: (a) Change handleClaudeCheck to actually spawn Claude (with a minimal prompt), wait for MCP handshake, verify MCP callback registration, then kill the process. (b) Add a timeout (10s) for the connection test. (c) Show detailed status progression: 'Resolving binary...' → 'Spawning Claude...' → 'Testing MCP...' → 'Connected!' or error. (d) Display Claude provider type (claude CLI, ollama, etc.) and version. (e) Cache successful test — don't re-test on every strategy toggle. (f) On failure, show actionable error message (e.g., 'Claude not found — install claude CLI or configure CLAUDE_COMMAND'). (g) Make the process async (non-blocking) using the Promise+poll pattern. (h) Clean up spawned Claude process on completion or timeout.",
      "acceptance": "Test Connection spawns Claude, verifies MCP handshake, and reports success with version info. Failure after timeout shows actionable error. UI responsive during test. Process cleaned up. Cached result avoids redundant tests.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (handleClaudeCheck — search: check-claude, line ~2018)",
          "internal/command/pr_split_09_claude.js (ClaudeCodeExecutor — resolve, spawn, isAvailable)"
        ],
        "references": [
          "internal/command/pr_split_10_pipeline.js (MCP callback registration pattern)"
        ],
        "dependencies": [
          "T30"
        ]
      }
    },
    {
      "task": "T36: Auto-attach Claude pane when Claude spawns during wizard",
      "description": "Currently the user must manually open the Claude split-view (Ctrl+L) to see Claude's output. Claude should auto-attach with visual notification. Changes: (a) When automatedSplit spawns Claude (in the pipeline), emit an event or set a flag: s.claudeAttached = true. (b) In handleAutoSplitPoll, detect s.claudeAttached transition (false→true) and auto-enable split-view if terminal has enough height (>=12 lines). (c) Show transient notification: 'Claude connected — Ctrl+L to toggle, Ctrl+] for full terminal'. (d) Auto-start screenshot polling. (e) If split-view was manually closed, don't re-open (track s.claudeManuallyDismissed). (f) Show a persistent Claude badge in status bar when Claude is running — clickable to open split-view.",
      "acceptance": "When auto strategy spawns Claude, split-view opens automatically showing Claude output. Notification visible for 3s. Manually closing split-view stays closed. Status bar shows Claude badge. Badge clickable. Small terminals skip auto-open.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (handleAutoSplitPoll, status bar rendering)",
          "internal/command/pr_split_15_tui_views.js (renderStatusBar — Claude badge)",
          "internal/command/pr_split_10_pipeline.js (automatedSplit — Claude spawn point)"
        ],
        "references": [],
        "dependencies": [
          "T28"
        ]
      }
    },
    {
      "task": "T37: Claude inline feedback during PLAN_GENERATION — detect questions, send answers",
      "description": "During PLAN_GENERATION with auto strategy, Claude may ask clarification questions (visible in its terminal output). Enhance the experience: (a) In handleAutoSplitPoll, detect when Claude's screenshot contains question markers (?, '>', 'please', 'clarif', 'would you') combined with Claude being idle (no recent output for >2s). (b) When detected, show an inline prompt at the bottom of the analysis screen: 'Claude is asking: [last line]. Type your response:'. (c) User types response, Enter sends via tuiMux.write(response + '\\r'). (d) Claude pane auto-scrolls to show the interaction. (e) Track conversation in s.claudeConversations[] for history. (f) Add keyboard shortcut (Ctrl+R or Enter when question detected) to focus the input field.",
      "acceptance": "Claude questions detected and surfaced inline without user opening split-view. User can respond directly. Response sent to Claude. Claude pane scrolls. History tracked. Non-blocking.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (handleAutoSplitPoll — Claude output analysis)",
          "internal/command/pr_split_15_tui_views.js (viewAnalysisScreen — inline prompt)"
        ],
        "references": [
          "internal/command/pr_split_16_tui_core.js (updateClaudeConvo — existing conversation pattern)"
        ],
        "dependencies": [
          "T29",
          "T36"
        ]
      }
    },
    {
      "task": "T38: 'Ask Claude' on PLAN_REVIEW with inline response rendering",
      "description": "The PLAN_REVIEW screen has an 'Ask Claude' button (T16). Enhance for richer interaction: (a) When clicked, open inline text input below the plan cards. (b) Pre-fill with context: 'Regarding split [selected split name]: '. (c) On Enter, send prompt to Claude via sendToHandle. (d) Show 'Waiting for Claude...' spinner. (e) Parse Claude's MCP response (waitForLogged with a 'planFeedback' tool) and display inline. (f) If Claude suggests plan modifications, offer 'Apply Suggestions' button that auto-edits the plan. (g) Track Q&A history in s.planQA[]. (h) Keep the conversation context (multiple rounds).",
      "acceptance": "Ask Claude opens text input with context. Sends prompt. Claude response rendered inline in styled card. Apply Suggestions modifies plan. Multi-round conversation. Non-blocking. Cancel works.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (handleFocusActivate for ask-claude zone)",
          "internal/command/pr_split_15_tui_views.js (viewPlanReviewScreen — Ask Claude section)"
        ],
        "references": [
          "internal/command/pr_split_10_pipeline.js (sendToHandle, waitForLogged)"
        ],
        "dependencies": [
          "T29",
          "T36"
        ]
      }
    },
    {
      "task": "T39: Claude context injection in ERROR_RESOLUTION with diagnostic info",
      "description": "When entering ERROR_RESOLUTION, Claude should receive rich diagnostic context automatically. Changes: (a) On transition to ERROR_RESOLUTION, if Claude is alive: compile error context (error message, stack trace, affected branch, recent git log, build output). (b) Send context to Claude via sendToHandle as a structured prompt: 'A build error occurred in branch X: [error]. The build output was: [output]. Please analyze this error and suggest a fix.' (c) Wait for Claude's response via waitForLogged (with 'errorDiagnosis' tool). (d) Display Claude's diagnosis inline on the ERROR_RESOLUTION screen. (e) If Claude suggests fix commands, offer 'Apply Fix' button. (f) If applying fix: run commands via exec.spawn (non-blocking), show CaptureSession output, re-verify. (g) Show Claude's confidence level if available.",
      "acceptance": "Error context auto-sent to Claude on ERROR_RESOLUTION entry. Claude's diagnosis displayed inline. Fix commands executable. Re-verification automatic. Non-blocking throughout. Fallback to manual if Claude unavailable.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (handleErrorResolutionState, handleErrorResolutionChoice)",
          "internal/command/pr_split_15_tui_views.js (viewErrorResolutionScreen)",
          "internal/command/pr_split_08_conflict.js (resolveConflicts)"
        ],
        "references": [
          "internal/command/pr_split_10_pipeline.js (sendToHandle, waitForLogged)"
        ],
        "dependencies": [
          "T31",
          "T36"
        ]
      }
    },
    {
      "task": "T40: Audit and fix tab navigation completeness across ALL screens",
      "description": "Tab/Shift+Tab navigation must reach EVERY focusable element on EVERY screen. Systematic audit: (a) For each wizard state (CONFIG, PLAN_GENERATION, PLAN_REVIEW, PLAN_EDITOR, BRANCH_BUILDING, EQUIV_CHECK, FINALIZATION, ERROR_RESOLUTION), enumerate ALL interactive elements (buttons, toggles, strategy selectors, split cards, file checkboxes, expand/collapse toggles). (b) Compare against getFocusElements() arrays — identify any missing elements. (c) Add missing elements to getFocusElements(). (d) Verify wrap-around works at boundaries for every screen. (e) Verify Enter activates the correct action for every focused element. (f) Verify visual focus indicator renders on every focusable element. (g) Add test: TestTabNavigationCompleteness — iterate all screens, verify element count matches expected.",
      "acceptance": "Tab reaches every interactive element on every screen. Shift+Tab reverses. Wrap-around works. Enter activates correct action. Visual focus visible on all. Test verifies count-parity between rendered elements and getFocusElements() arrays.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (getFocusElements — line ~1619)",
          "internal/command/pr_split_15_tui_views.js (all viewXxxScreen functions)"
        ],
        "references": [
          "internal/command/super_document_script.js (focus pattern reference)"
        ],
        "dependencies": []
      }
    },
    {
      "task": "T41: Fix expand/collapse interactions for verification output, advanced options, split details",
      "description": "Expand/collapse UI pattern has multiple issues: clicking to expand works but collapse may not, scrolled viewports affect zone detection, and some expand toggles lack visual indicators. Audit and fix: (a) Verification output expand/collapse (verify-expand-{branch}/verify-collapse-{branch} zones): verify collapse actually clears s.expandedVerifyBranch. Test with scrolled viewport. (b) Advanced options on CONFIG screen: verify expand/collapse toggle for advanced settings panel. (c) Split detail expansion on PLAN_REVIEW: clicking a split card should expand/collapse file list. Verify s.expandedSplitIdx toggles correctly. (d) Add explicit collapse affordance (▼/▶ chevron, 'Show less' text). (e) Ensure Escape collapses any expanded section before navigating back. (f) Test all expand/collapse interactions via VTerm observation.",
      "acceptance": "Every expand/collapse interaction works bidirectionally. Visual chevrons indicate state. Escape collapses before back-nav. Scrolled viewport doesn't break zone detection. All interaction paths tested.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (handleScreenMouseClick — verify-expand/collapse zones at ~line 1264)",
          "internal/command/pr_split_15_tui_views.js (viewExecutionScreen, viewConfigScreen, viewPlanReviewScreen)"
        ],
        "references": [],
        "dependencies": []
      }
    },
    {
      "task": "T42: Add loading indicators for all async operations",
      "description": "Several async operations lack visual feedback beyond progress bars. Add spinners and contextual loading indicators: (a) Claude connection test: spinning dots with status text. (b) Analysis steps: per-step spinner (⠋⠙⠹⠸⠼⠴⠦⠧⠇⠏ cycle). (c) Branch creation: per-branch spinner. (d) Equivalence check: spinner with 'Checking tree hashes...' text. (e) Claude response wait: spinner with 'Waiting for Claude...' and elapsed time. (f) Implement spinner as BubbleTea tick-based animation (100ms cycle). (g) Add s.spinnerFrame counter, increment on tick.",
      "acceptance": "Every async operation shows a spinner while waiting. Spinner animates smoothly (no flicker). Spinners stop immediately when operation completes. No render glitches. Consistent spinner style across all screens.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_15_tui_views.js (all screen renderers)",
          "internal/command/pr_split_16_tui_core.js (tick handlers)"
        ],
        "references": [
          "internal/command/super_document_script.js (spinner patterns if any)"
        ],
        "dependencies": []
      }
    },
    {
      "task": "T43: Claude output search — Ctrl+F in Claude pane with match highlighting",
      "description": "Add search functionality to the Claude pane (similar to terminal search): (a) Ctrl+F when Claude pane is focused opens search bar at top of Claude pane. (b) Text input with live incremental search. (c) Matches highlighted with inverse colors in the rendered output. (d) n/N to cycle through matches. (e) Esc to close search. (f) Match count displayed. (g) Search works on plain-text buffer (from screenshot/output) even though rendered in ANSI. (h) Implement as overlay within the Claude pane bounds.",
      "acceptance": "Ctrl+F opens search bar. Typing filters/highlights matches. n/N cycles. Esc closes. Match count displayed. Works on ANSI-rendered content without garbling. Performance acceptable on large buffers.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (Claude pane key handler)",
          "internal/command/pr_split_15_tui_views.js (renderClaudePane — add search overlay)"
        ],
        "references": [],
        "dependencies": [
          "T28",
          "T29"
        ]
      }
    },
    {
      "task": "T44: Integration test: UI responsiveness during pipeline (cancel within 1s)",
      "description": "Write integration tests that verify the BubbleTea UI remains responsive during asynchronous pipeline operations. These MUST exercise the non-blocking conversions from T31-T34. Tests: (a) TestIntegration_UIResponsiveDuringAnalysis: start heuristic analysis, send TickMsg during analysis, verify view renders (non-empty output). (b) TestIntegration_UIResponsiveDuringExecution: start branch creation, verify tick processing during creation. (c) TestIntegration_CancelDuringPipeline: start auto-split pipeline, send Ctrl+C after 500ms, verify cancel processed within 1s. (d) TestIntegration_ClaudeScreenshotDuringPipeline: start auto-split, verify claude-screenshot ticks produce output while pipeline runs. (e) All tests use mock MCP or minimal Claude to avoid external dependencies. (f) Add to integration-test-prsplit-mcp target.",
      "acceptance": "Tests verify non-blocking behavior. Cancel processed within 1s. View renders during pipeline. Claude screenshots update during pipeline. All tests pass. No external dependencies (except mock Claude for test d).",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_responsiveness_test.go (new file)",
          "project.mk (integration-test-prsplit-mcp target)"
        ],
        "references": [
          "internal/command/pr_split_wizard_integration_test.go (existing wizard test patterns)"
        ],
        "dependencies": [
          "T32",
          "T33",
          "T34"
        ]
      }
    },
    {
      "task": "T45: VTerm integration tests for Claude pane ANSI rendering",
      "description": "Write VTerm-based observation tests that verify Claude pane renders ANSI correctly. Tests: (a) TestVTerm_ClaudePaneANSIColors: spawn Claude-like process that produces colored output, verify Claude pane contains ANSI sequences in render. (b) TestVTerm_ClaudePanePlainFallback: spawn process with plain output, verify fallback rendering. (c) TestVTerm_ClaudePaneScrolling: produce large output, verify scroll offset changes with keyboard. (d) TestVTerm_ClaudePaneFocusIndicator: verify border color changes when focus switches. (e) All use CaptureSession-based VTerm observation (not real Claude).",
      "acceptance": "ANSI rendering verified in VTerm screenshots. Scrolling works. Focus indicator visible. Plain fallback works. Tests use mock processes. All pass on unix.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_claude_pane_test.go (new file)"
        ],
        "references": [
          "internal/command/pr_split_termmux_observation_test.go (VTerm test patterns)"
        ],
        "dependencies": [
          "T28"
        ]
      }
    },
    {
      "task": "T46: VTerm integration tests for Claude pane keyboard input forwarding",
      "description": "Write VTerm-based tests that verify keyboard input reaches Claude when Claude pane is focused. Tests: (a) TestVTerm_ClaudePaneKeyInput: open split-view, switch focus to Claude, type 'hello\\r', verify Claude process receives input (mock process echoes). (b) TestVTerm_ClaudePaneReservedKeys: verify Ctrl+L, Tab, Ctrl+C NOT forwarded to Claude. (c) TestVTerm_ClaudePanePaste: send multi-char paste, verify received as single chunk. (d) TestVTerm_ClaudePaneBackspace: verify backspace (\\x7f) forwarded correctly.",
      "acceptance": "Keyboard input forwarding verified end-to-end via VTerm observation. Reserved keys stay with wizard. Paste works. Backspace works. Tests use mock echo process.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_claude_pane_test.go"
        ],
        "references": [
          "internal/command/pr_split_termmux_observation_test.go (VTerm test patterns)"
        ],
        "dependencies": [
          "T29"
        ]
      }
    },
    {
      "task": "T47: End-to-end Claude integration test: full wizard flow with real Claude",
      "description": "Write a comprehensive integration test that exercises the FULL wizard flow with a real Claude process. This is the ultimate validation that everything works together: (a) TestIntegration_FullWizardFlowRealClaude: create test repo with 10+ files in 3 categories, start wizard with auto strategy, verify: Claude spawns, split-view auto-opens, classification completes, plan generated, plan review displays correct splits, execute splits, verify equivalence passes, finalization shows report. (b) Test must use real Claude (via integration flag) with the PRSPLIT test infrastructure. (c) Timeout: 10 minutes. (d) Assert intermediate states: analysis progress updates, Claude screenshot non-empty, plan has >=3 splits. (e) Skip cleanly when Claude unavailable.",
      "acceptance": "Full wizard flow completes successfully with real Claude. All intermediate states verified. Claude pane rendered. Classification accurate. Equivalence passes. Takes <10min. Clean skip without Claude.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_integration_test.go"
        ],
        "references": [
          "internal/command/pr_split_local_integration_test.go (existing real-Claude test patterns)",
          "project.mk (integration-test-prsplit target)"
        ],
        "dependencies": [
          "T31",
          "T36"
        ]
      }
    },
    {
      "task": "T48: Integration test: Claude pane auto-attach on spawn",
      "description": "Test that Claude pane auto-attaches when Claude spawns. Tests: (a) TestIntegration_ClaudePaneAutoAttach: start auto strategy, verify split-view enables within 5s of Claude spawn. (b) TestIntegration_ClaudePaneAutoAttach_SmallTerminal: set terminal to 10 lines, verify split-view NOT auto-opened (too small). (c) TestIntegration_ClaudePaneManualDismiss: auto-attach, dismiss with Ctrl+L, verify not re-opened. (d) TestIntegration_ClaudeBadge: verify status bar shows Claude badge when running.",
      "acceptance": "Auto-attach works on normal terminals. Skipped on small terminals. Manual dismiss respected. Badge visible. All use mock Claude.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_claude_pane_test.go"
        ],
        "references": [
          "internal/command/pr_split_wizard_integration_test.go"
        ],
        "dependencies": [
          "T36"
        ]
      }
    },
    {
      "task": "T49: Benchmark tests for view rendering performance",
      "description": "Add benchmarks for view rendering: (a) Config/PlanReview/PlanReviewLarge(50 splits)/Execution/Finalization views. (b) Assert <16ms standard, <50ms large (60fps budget). (c) Track allocs/op. (d) Claude pane ANSI rendering benchmark. (e) Split-view rendering benchmark. (f) Add to project.mk bench-prsplit target.",
      "acceptance": "Benchmarks exist and run. Standard views <16ms. Large <50ms. Can track regressions. project.mk target works.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_benchmark_test.go (new file)"
        ],
        "references": [
          "internal/termmux/vt/bench_test.go (benchmark patterns)"
        ],
        "dependencies": [
          "T28"
        ]
      }
    },
    {
      "task": "T50: Edge-case hardening tests for pipeline robustness",
      "description": "Expand edge cases: (a) Empty diff. (b) Single file. (c) Only deletions. (d) Binary files. (e) 10,000 files. (f) Special chars in paths. (g) Branch collision. (h) Concurrent instances. All must handle gracefully with non-blocking operations.",
      "acceptance": "All 8 pass. Empty=early exit. Single=1 split. Binary=graceful. Large=no OOM. Special=safe. Collision=error. Concurrent=rejected.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_edge_hardening_test.go (new file)"
        ],
        "references": [],
        "dependencies": []
      }
    },
    {
      "task": "T51: Fuzz tests for classification parsing and plan validation",
      "description": "Add fuzz tests: (a) FuzzClassificationParsing. (b) FuzzSplitPlanValidation. (c) FuzzPromptRendering. (d) Seed from real responses. (e) Add fuzz-prsplit target to project.mk.",
      "acceptance": "30s each no panics. >=5 seeds each. project.mk target. All stable.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_fuzz_test.go (new file)",
          "project.mk"
        ],
        "references": [
          "internal/termmux/vt/ (fuzz test patterns)"
        ],
        "dependencies": []
      }
    },
    {
      "task": "T52: Drastically rewrite docs/pr-split-tui-design.md with Claude integration flows",
      "description": "The design doc is stale and doesn't reflect the current architecture. DRASTIC rewrite: (a) Remove all outdated sections. (b) Add: 17-chunk architecture overview with dependency graph. (c) Add: ANSI Claude pane rendering architecture (Mux.ChildScreen → renderClaudePane → BubbleTea viewport). (d) Add: Claude pane input forwarding flow (keypress → tuiMux.write → child PTY). (e) Add: Non-blocking operation architecture (exec.spawn → Promise → poll pattern). (f) Add: Interactive Claude flows per wizard phase (CONFIG: test connection, PLAN_GENERATION: inline Q&A, PLAN_REVIEW: ask Claude, ERROR_RESOLUTION: diagnostics). (g) Add: State machine diagram with all transitions. (h) Add: Complete keyboard shortcuts table. (i) Add: Layout diagram for split-view (wizard + Claude + divider). (j) Add: Signal handling flow. (k) Reference all relevant source files. (l) Proofread for accuracy against current code.",
      "acceptance": "Design doc accurately reflects current implementation including ANSI rendering, input forwarding, non-blocking operations, and Claude integration flows. No stale references. Useful for onboarding new developers. All source file references verified accurate.",
      "status": "Not Started",
      "context": {
        "files": [
          "docs/pr-split-tui-design.md"
        ],
        "references": [
          "docs/architecture-pr-split-chunks.md",
          "internal/command/pr_split.go",
          "internal/command/pr_split_16_tui_core.js",
          "internal/command/pr_split_15_tui_views.js"
        ],
        "dependencies": [
          "T28",
          "T29",
          "T31",
          "T36",
          "T37",
          "T38",
          "T39"
        ]
      }
    },
    {
      "task": "T53: Update docs/architecture-pr-split-chunks.md with current 17-chunk inventory",
      "description": "Update: (a) All 17 chunks with line counts, exports, dependencies. (b) IIFE pattern documentation. (c) Dependency matrix. (d) Embed directives. (e) Adding-chunk guide. (f) Deadcode notes. (g) Note async patterns (exec.spawn, Promise+poll). (h) Note ANSI rendering path.",
      "acceptance": "17 chunks accurately listed. Matrix complete. Guidelines actionable. Consistent with pr_split.go chunk loading. Async patterns documented.",
      "status": "Not Started",
      "context": {
        "files": [
          "docs/architecture-pr-split-chunks.md",
          "internal/command/pr_split.go"
        ],
        "references": [],
        "dependencies": [
          "T31"
        ]
      }
    },
    {
      "task": "T54: Audit REPL mode output.print gating",
      "description": "Verify: (a) output.print only in REPL mode. (b) Mode selection gates correctly. (c) Document contract. (d) Verify that REPL commands still use synchronous gitExec (not the new gitExecAsync) since blocking is acceptable in REPL mode.",
      "acceptance": "Verified unreachable from BubbleTea. Documented. grep confirms. REPL still uses gitExec.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (registerMode section)",
          "internal/command/pr_split_14_tui_commands.js"
        ],
        "references": [],
        "dependencies": [
          "T31"
        ]
      }
    },
    {
      "task": "T55: Session persistence — wizard state checkpoint and resume after crash/cancel",
      "description": "Checkpoint system: (a) Save wizard state on every state transition (JSON to ~/.config/osm/pr-split-session.json). (b) On startup, check for session file — offer 'Resume' or 'Start Fresh'. (c) State includes: wizard state, plan cache, analysis cache, groups cache, execution progress, error context, Claude session info. (d) Delete session file on successful completion (FINALIZATION→DONE). (e) Session lock file to prevent concurrent pr-split instances on same repo. (f) Handle stale locks (check PID).",
      "acceptance": "Cancel→restart offers Resume. Crash→restart offers Resume. Completion deletes session. Start Fresh clears session. Lock prevents concurrent. Stale locks handled.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js",
          "internal/command/pr_split_13_tui.js"
        ],
        "references": [
          "internal/session/",
          "internal/storage/"
        ],
        "dependencies": []
      }
    },
    {
      "task": "T56: Multiple Claude session management — named sessions, switchable panes",
      "description": "Support multiple named Claude sessions that can be switched between: (a) Add s.claudeSessions map keyed by name (e.g., 'analysis', 'review', 'resolution'). (b) Each session has its own CaptureSession/Mux attachment. (c) Claude pane shows session selector (tabs or dropdown). (d) Switch between sessions via Ctrl+1..9 or Tab when session selector is focused. (e) Kill/restart individual sessions. (f) Session-specific history. (g) Default: single 'main' session (backward compatible).",
      "acceptance": "Multiple Claude sessions creatable and switchable. Each has independent stdout/buffer. Session selector visible. Ctrl+1..9 switches. Kill/restart per session. Single-session backward compatible.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js",
          "internal/command/pr_split_15_tui_views.js (Claude pane session selector)"
        ],
        "references": [
          "internal/builtin/termmux/module.go"
        ],
        "dependencies": [
          "T28",
          "T29"
        ]
      }
    },
    {
      "task": "T57: Cross-platform validation — macOS native, Linux container, Windows",
      "description": "Verify all functionality works across platforms: (a) Run make-all-with-log on macOS (native). (b) Run make-all-in-container on Linux (Docker). (c) Run make-all-run-windows on Windows (Docker). (d) File platform-specific bugs for any failures. (e) Fix all failures. (f) Verify CaptureSession works on all platforms. (g) Verify signal handling on all platforms. (h) Verify terminal rendering on all platforms.",
      "acceptance": "All three platform targets pass. No platform-specific regressions. CaptureSession works. Signals work. Rendering correct.",
      "status": "Not Started",
      "context": {
        "files": [
          "config.mk (make-all-with-log, make-all-in-container, make-all-run-windows)"
        ],
        "references": [],
        "dependencies": [
          "T34"
        ]
      }
    },
    {
      "task": "T58: Render cycle performance profiling — find and fix hot paths",
      "description": "Profile the TUI render cycle to identify and fix performance bottlenecks: (a) Add instrumentation to _viewFn to measure total render time. (b) Profile splitView rendering (both panes + divider). (c) Profile ANSI rendering in Claude pane. (d) Identify any O(n²) patterns in view rendering. (e) Optimize hot paths (cache computed values, reduce allocations). (f) Verify 60fps target (<16ms per frame) under normal load.",
      "acceptance": "Render cycle instrumented. Hot paths identified. Optimized to <16ms standard, <50ms worst case. No O(n²) patterns. 60fps achievable.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_16_tui_core.js (_viewFn)",
          "internal/command/pr_split_15_tui_views.js"
        ],
        "references": [],
        "dependencies": [
          "T49"
        ]
      }
    },
    {
      "task": "T59: Accessibility review — contrast ratios, keyboard-only workflow completeness",
      "description": "Review and fix accessibility: (a) Re-verify WCAG AA contrast ratios for new ANSI-rendered content. (b) Verify complete keyboard-only workflow (no mouse required at any point). (c) Add descriptive alt-text equivalents for icons/badges (accessible via ? help). (d) Verify screen reader compatibility hints (if applicable to TUI). (e) Test with high-contrast terminal theme. (f) Test with light terminal theme.",
      "acceptance": "All contrast ratios pass WCAG AA. Complete keyboard-only workflow. High-contrast and light themes render correctly. Help text describes all visual indicators.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_15_tui_views.js (COLORS, styles)"
        ],
        "references": [],
        "dependencies": [
          "T28",
          "T40"
        ]
      }
    },
    {
      "task": "T60: Visual polish pass — smooth scrolling, transition animations, refinement",
      "description": "Final visual polish: (a) Smooth scroll transitions (interpolated viewport jumps instead of instant). (b) Fade-in for overlay open (progressive render over 2-3 frames). (c) Consistent padding and alignment across all screens. (d) Icon consistency (all screens use same emoji/unicode style). (e) Whitespace balance: no cramped or sparse areas. (f) Final typography review: heading hierarchy, label styles, data formatting. (g) Screenshot documentation of every screen state for docs/.",
      "acceptance": "Scroll transitions smooth. Overlays animate in. Padding consistent. Icons consistent. Typography clean. Screenshots captured for documentation.",
      "status": "Not Started",
      "context": {
        "files": [
          "internal/command/pr_split_15_tui_views.js",
          "internal/command/pr_split_16_tui_core.js"
        ],
        "references": [],
        "dependencies": [
          "T52"
        ]
      }
    },

    {
      "task": "T63: Update CHANGELOG.md with all changes from T27-T62",
      "description": "Update CHANGELOG.md following Keep a Changelog 1.1.0 specification: (a) Add [Unreleased] section (or update existing). (b) 'Added' entries: Mux.ChildScreen ANSI rendering, Mux.WriteToChild input forwarding, Claude pane ANSI display, Claude pane keyboard interaction, gitExecAsync non-blocking git, waitForAsync non-blocking MCP wait, Claude auto-attach, Claude inline feedback, Claude search (Ctrl+F), session persistence, multiple Claude sessions. (c) 'Changed' entries: Pipeline converted to non-blocking, runAnalysisStep async, runExecutionStep per-branch yield, verifyEquivalence non-blocking, 'Test Connection' now tests real MCP, design docs rewritten. (d) 'Fixed' entries: Event loop blocking during pipeline, tab navigation gaps, expand/collapse interactions. (e) Proofread all entries for accuracy.",
      "acceptance": "CHANGELOG.md follows Keep a Changelog format. All T27-T62 changes have entries. Entries are accurate. No stale content.",
      "status": "Not Started",
      "context": {
        "files": [
          "CHANGELOG.md"
        ],
        "references": [],
        "dependencies": [
          "T60"
        ]
      }
    }
  ],
  "continuousVerification": {
    "task": "Run `make` after EVERY significant change",
    "description": "Execute `make` which runs build, lint (vet, staticcheck, betteralign, deadcode), and test with race detection. ALL must pass on ALL platforms (ubuntu, windows, macos). Use `make test` for fast feedback, `make` for full verification before commit.",
    "failPolicy": "If ANY check fails, STOP IMMEDIATELY and fix before proceeding.",
    "testCoverage": "Verify coverage for new code using `make cover`. Target >=90% for TUI functions, 100% for state machine transitions."
  },
  "finalEnforcementProtocol": [
    {
      "type": "POST_EXECUTION_WARNING",
      "alert": "ALL checks must pass. NEVER commit with failing checks."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "Rule of Two MANDATORY before every commit. Two contiguous issue-free reviews of the exact same diff."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "JS runs in Goja event loop. Never call exec.execv() in update() or Promise continuations. Use exec.spawn() + await. Use tea.tick() to yield."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "Claude pane MUST use ANSI rendering via Mux.ChildScreen()/vtm.RenderFullScreen(). plain-text screenshot() is only for diagnostics and test assertions."
    },
    {
      "type": "POST_EXECUTION_WARNING_REPEATED",
      "alert": "Interactive Claude keyboard input forwarding via Mux.WriteToChild() when Claude pane is focused. Reserve Ctrl+L/Tab/Ctrl+C/Ctrl+]/F1/? for wizard control."
    }
  ]
}
