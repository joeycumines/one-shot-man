{
  "meta": {
    "fileName": "blueprint.json",
    "purpose": "Drive the termmux / pr-split redesign to deliver a flawless, exemplary implementation of osm pr-split: fully interactive Claude + verification + shell panes with persistent session model, cross-platform support (macOS, Linux, Windows), and exhaustive test coverage. All tasks verified through Rule of Two before marking Done.",
    "maintenanceInstruction": "KEEP THE PLAN UP TO DATE AS YOU EVOLVE THE CODE. DEVIATIONS TO THE PLAN MUST BE LOGGED WITHIN THE PLAN. THE PLAN MUST BE REASSESSED HOLISTICALLY AFTER ANY CHANGE OF ANY SIZE. This refined blueprint addresses quality issues identified in the Rule of Two analysis.",
    "completionPolicy": "YOU WILL COMPLETE 100% OF THE BLUEPRINT. NO PRIORITIES. NO ESTIMATES. This is flat, sequential, and EXHAUSTIVE. All tasks are mandatory. Refine and expand indefinitely after reaching stable state."
  },
  "globalAlerts": [
    {
      "warning": "Use GNU Make and project custom targets in config.mk. Prefer make make-all-with-log, make test-prsplit-fast, make test-prsplit-all, make integration-test-prsplit-mcp, make integration-test-termmux, make make-all-in-container, make make-all-run-windows for verification. All checks must pass on all platforms before marking tasks Done."
    },
    {
      "warning": "Acceptance criteria must describe USER VALUE and end-to-end functionality, not just architectural changes. Every task must answer: 'What can the user or system now do that it couldn't before?' A task is only Done when acceptance is fully satisfied and verified through Rule of Two."
    },
    {
      "warning": "JS and Go API parity is mandatory. Do not leave JavaScript call sites pretending termmux supports APIs that the Go binding does not actually expose. Every API surface must be honest and tested."
    },
    {
      "warning": "Cross-platform is non-negotiable. The feature must work on macOS, Linux, and Windows. Windows ConPTY is a HIGH priority requirement and must be solved, not worked around."
    },
    {
      "warning": "No prepositions in method or function names (e.g. prefer LoadConfig over LoadFromConfig)."
    }
  ],
  "mandatoryDirectives": {
    "contextManagement": "Execute work in coherent vertical slices. Each task is cold-start-safe with explicit file targets, dependencies, and end-to-end acceptance. Keep the blueprint flat and sequential.",
    "implementationStandard": "The goal is an exemplary, human-quality implementation with zero apparent AI slop. This means: code that reads like a human wrote it, tests that are exhaustive not approximate, error handling that is explicit not hand-waved, and end-to-end workflows that are proven to work, not speculated.",
    "reviewGate": "Every task must pass two contiguous Rule of Two reviews (subagent verification) before marking Done. Code quality is verified through make make-all-with-log and project-specific integration tests. No task is complete without Rule of Two gate."
  },
  "statusSection": {
    "currentState": "Blueprint updated to resolve Rule of Two Pass 1 failure (attempt 4). A strict replanning checkpoint has been inserted after Task 6 to ensure the agent discovers the new modular TUI structure rather than guessing. Dependent tasks (Verify session leak, TUI keymap) have been updated to explicitly depend on the replan and no longer hallucinate file dependencies. Task 10 execution primitives are generalized to prevent cross-platform violations.",
    "goalState": "Level 1 (Functional): User can run osm pr-split, interact with Claude pane, verification shell, and user shell interactively — inline and fullscreen passthrough — with seamless switching through a true multi-session manager. Windows ConPTY is fully supported. Level 2 (Quality): All interactions are responsive, rendering is pixel-perfect (no duplicate output, correct borders), focus system is reliable, error states are recoverable, code is exemplary (reads human-written), tests are exhaustive (no build tag violations). Level 3 (Validation): End-to-end tests prove workflows on all platforms; CI green on all checks; no performance regressions; no AI slop.",
    "definitionOfDone": "The blueprint is Done when: (1) ALL tasks are marked Done, (2) each Done task has passed two contiguous Rule of Two reviews, (3) zero-uncovered-code-paths exist for the new functionality, (4) manual smoke tests confirm behavior across platforms, (5) docs are fully updated, and (6) all integration tests pass on all CI platforms.",
    "replanLog": [
      {
        "trigger": "Rule of Two Pass 1 Failure (attempt 4) - structural unknown violation",
        "affectedTasks": "Tasks 7-15",
        "summary": "Inserted Replanning Checkpoint after Task 6 to respect 'Surface unknowns explicitly' constraint. Stripped explicit file expectations from downstream TUI tasks so they depend on the replan. Corrected Linux-specific prescriptiveness in process termination task."
      },
      {
        "trigger": "Rule of Two Pass 1 Failure (attempt 3) - autopsy-driven pivot",
        "affectedTasks": "Global rewrite",
        "summary": "Rewrote blueprint to incorporate autopsy insights. Falsely completed tasks and critical gaps converted back to Not Started. Added replanLog. Fixed Task 9 logical paradox. Added Task 3 dependency to Task 5. Enforced testing.Short() in Task 11 E2E tests."
      }
    ]
  },
  "sequentialTasks": [
    {
      "id": 1,
      "dependsOn": [],
      "task": "Add CaptureSession Interface Assertion (GAP-006)",
      "description": "Add a compile-time interface assertion `var _ InteractiveSession = (*CaptureSession)(nil)` to internal/termmux/capture.go to ensure it structurally implements InteractiveSession and prevent future silent API breakages.",
      "acceptance": "The build system structurally rejects any future refactors that break CaptureSession's interactive capabilities without corresponding API updates, ensuring developers immediately catch InteractiveSession contract violations at the compiler level rather than at runtime.",
      "status": "Not Started"
    },
    {
      "id": 2,
      "dependsOn": [],
      "task": "Implement ConPTY Windows support (GAP-005)",
      "description": "Windows PTY support is missing, currently dropping Windows to capture-only mode. Implement full ConPTY support in internal/termmux/pty/ and internal/termmux/platform_windows.go to bring interactive parity to Windows users.",
      "acceptance": "pr-split verify shell, Claude pane, and user shell are fully interactive on Windows via ConPTY, with seamless functional parity to macOS and Linux.",
      "status": "Not Started"
    },
    {
      "id": 3,
      "dependsOn": [1, 2],
      "task": "Build a real multi-session manager in Go (GAP-001/007)",
      "description": "Build a `SessionManager` in internal/termmux/sessions.go (and tests in internal/termmux/sessions_test.go) using bigbuff.ChanPubSub/ChanCaster. Write separate JS shims in internal/builtin/termmux/module.go (osm:termmux). The manager must track multiple InteractiveSessions via map, handle active session routing, broadcast resizes, and aggregate events (output, bell, done) across sessions. Do NOT use prepositions in any method/function names (e.g., use LoadConfig, not LoadFromConfig). Ensure JS code can natively register and switch sessions.",
      "acceptance": "A centralized Go-level SessionManager handles a map of sessions, routing input/output, and aggregating events. JS shims provide a transparent API overlay. The Mux is no longer implicitly assumed to be single-child for interactivity.",
      "status": "Not Started"
    },
    {
      "id": 4,
      "dependsOn": [3],
      "task": "Implement fullscreen passthrough for any interactive session (GAP-008)",
      "description": "Currently, only Claude gets true Mux passthrough. Implement true fullscreen passthrough functionality for the Verify Pane and user shell via CaptureSession. Modify internal/command/pr_split_16f_tui_model.js to handle the frontend input routing for Ctrl+], mapping the keystrokes to the correct actively-focused backend session interface. Update internal/termmux/capture.go and the corresponding JS shim in internal/builtin/termmux/module.go to process the request.",
      "acceptance": "Users can hit Ctrl+] from the Verify Pane or user shell and get a fullscreen passthrough experience (not just inline). Session switching works flawlessly while in passthrough.",
      "status": "Not Started"
    },
    {
      "id": 5,
      "dependsOn": [3],
      "task": "Support 'Ask Claude' in pr-split regardless of auto mode (GAP-002)",
      "description": "The 'Ask Claude' button in pr-split throws an error in non-auto modes. Update the TUI handlers (e.g. pr_split_16d_tui_handlers_claude.js) so that the 'Ask Claude' workflow spawns a Claude executor on-demand and works whenever the button is displayed, regardless of the user's strategy mode.",
      "acceptance": "Clicking 'Ask Claude' successfully opens the Claude session and allows the user to get help in all pr-split modes (auto, manual, heuristic, directory). No auto-mode gating for this feature.",
      "status": "Not Started"
    },
    {
      "id": 6,
      "dependsOn": [3],
      "task": "Decompose wizardUpdateImpl god function (KILL-004)",
      "description": "Decompose the ~600-line wizardUpdateImpl god function in pr_split_16e_tui_update.js into per-message-type handler dispatch functions. Separate key messages, mouse messages, window resizes, tick messages, and overlays into independently testable components.",
      "acceptance": "wizardUpdateImpl is reduced to a concise, top-level dispatch function. The monolithic structure is refactored into modular handlers with zero behavioral regressions.",
      "status": "Not Started"
    },
    {
      "id": 7,
      "dependsOn": [6],
      "task": "Checkpoint: Replan TUI routing tasks based on new modular structure",
      "description": "Research Task & Replanning Checkpoint: Inspect the output of Task 6 to map the new modular handler structure. Do not guess the new file names or function structures. Locate where verify messages, key captures, and pane switching now live in the refactored TUI. Re-write all subsequent TUI-dependent tasks (like Verify session leak fix and TUI keymap docs) with exact, concrete file pointers based on this new reality. Proceed to next task ONLY after the blueprint has been replanned.",
      "acceptance": "The planner has explored the post-Task-6 codebase and updated the blueprint. Dependent tasks are heavily revised to point to the actual, correct un-papered file and function targets. A Replan Log entry is recorded.",
      "status": "Not Started"
    },
    {
      "id": 8,
      "dependsOn": [],
      "task": "Fix border formatting around autodetect/main values for source and target branch on PR split wizard configure page (GAP-003)",
      "description": "The borders surrounding the autodetect and main/default values for the source branch and target branch fields on the PR split wizard's configure page are broken and clip/wrap based on terminal constraints. Write a failing test that captures the broken rendering at multiple terminal sizes. Then fix the border rendering code in pr_split_15c_tui_screens.js so the test passes. Ensure borders render correctly at any terminal size without clipping or wrapping incorrectly.",
      "acceptance": "Configure page renders cleanly with correctly-aligned borders around source and target branch fields at every terminal size. Golden-file tests exist proving the fix works across sizes.",
      "status": "Not Started"
    },
    {
      "id": 9,
      "dependsOn": [3, 7],
      "task": "Fix Verify session leak on branch re-order (KILL-001)",
      "description": "DEPENDS ON REPLAN (Task 7): First, execute the replan against the decomposed TUI. Then, in the identified modular handler, locate the logic that handles branch re-ordering. If a user changes the active branch split during verification, the old verifySession remains actively running on the old branch's worktree. Handle asynchronous pipeline cancellations properly to close the old session before establishing the new one by propagating cancellation to the SessionManager in internal/termmux/sessions.go.",
      "acceptance": "Moving between branch verification states cleanly shuts down the previous verify session and starts the new one without output leakage or hanging orphaned processes.",
      "status": "Not Started"
    },
    {
      "id": 10,
      "dependsOn": [3],
      "task": "Fix Claude orphan on double-SIGINT (KILL-002)",
      "description": "When a user hits double-SIGINT in internal/command/pr_split.go, os.Exit(130) runs without executing defers, skipping Claude executor cleanly and leaving Claude worker process groups orphaned. Intercept the double-SIGINT and trigger a synchronous context cancellation/waitgroup teardown *before* issuing os.Exit(130), using appropriate multi-platform primitives (such as handling process groups securely on Linux/macOS and Job Objects on Windows) to instruct the OS to terminate child processes in the event of abrupt host termination.",
      "acceptance": "Double Ctrl+C exits the program immediately but strictly guarantees all attached Claude worker process groups are killed natively or via synchronous teardown across Linux, macOS, and Windows without cross-platform build breakage; no orphaned background processes remain.",
      "status": "Not Started"
    },
    {
      "id": 11,
      "dependsOn": [],
      "task": "Remove -tags=integration build tag violation (GAP-009)",
      "description": "Remove the usage of `-tags=integration` and `+build integration` tags from the codebase. Rely exclusively on `testing.Short()` / `-short` flag for skipping slow execution, according to project rules, and update project.mk accordingly.",
      "acceptance": "Developers and CI systems can reliably partition fast vs. slow tests using standard Go test flags (`go test -short`) without needing custom build tags. Standard Go tooling (like gopls and IDE test runners) works flawlessly out of the box without requiring special build constraint configuration.",
      "status": "Not Started"
    },
    {
      "id": 12,
      "dependsOn": [3, 4],
      "task": "Implement E2E Claude integration test with a mock binary (GAP-010)",
      "description": "Current pipeline tests rely heavily on mock MCP servers rather than a true binary. Implement an end-to-end integration test in internal/command/pr_split_binary_e2e_test.go that uses a generic mock binary specifically representing the real Claude CLI to verify process management, full lifecycle connection, and termination. You MUST include a testing.Short() or skipSlow(t) guard at the top of the test function because it spawns subprocesses, strictly adhering to AGENTS.md test rules.",
      "acceptance": "An E2E test runs the overarching pr-split workflow by spawning a mock Claude binary locally, proving that process management works seamlessly with PTYs end-to-end. The test safely skips when 'go test -short' is run, ensuring fast feedback loops are not polluted.",
      "status": "Not Started"
    },
    {
      "id": 13,
      "dependsOn": [7],
      "task": "Concrete UX refinement and TUI keymap docs",
      "description": "DEPENDS ON REPLAN (Task 7): First, execute the replan to locate where TUI keys are registered and mapped in the post-Task-6 structure. Then make the TUI shortcuts mechanically verifiable: Implement Ctrl+Tab to navigate panes without eating input. Tie the '?' key to open a Lipgloss-rendered modal with all registered shortcuts by capturing the key and rendering it in internal/command/pr_split_15d_tui_dialogs.js. Add a markdown table to docs/reference/tui-api.md matching 1:1 with TUI keymap registers.",
      "acceptance": "Ctrl+Tab navigates panes without eating input; ? opens a Lipgloss-rendered modal with all registered shortcuts; Docs contain a markdown table matching 1:1 with TUI keymap registers.",
      "status": "Not Started"
    },
    {
      "id": 14,
      "dependsOn": [3, 4],
      "task": "Benchmark pane switching and render pipeline",
      "description": "Add a performance test using testing.B in internal/command/pr_split_16_bench_test.go that benchmarks the pane switching and rendering latency. Validate that switching between the Claude session and user shell takes < 50ms per switch. Use pprof if needed to isolate allocation bottlenecks in the render path.",
      "acceptance": "A benchmark test `BenchmarkPaneSwitching` runs locally in internal/command/pr_split_16_bench_test.go. Pprof output shows pane switch latency consistently below 50ms at scale with 1000 simulated log lines.",
      "status": "Not Started"
    },
    {
      "id": 15,
      "dependsOn": [3],
      "task": "Add fuzz testing to multi-session routing logic",
      "description": "Add a go fuzz test `FuzzSessionRouter` in internal/termmux/sessions_test.go to bombard the newly built SessionManager with parallel, random byte sequences and session-switch command interrupts to find race conditions.",
      "acceptance": "The SessionManager routing logic is proved practically to survive chaotic, interleaved IO from concurrent PTY sessions, guaranteeing that no sequence of rapid tab-switching by the user while background streams are active will ever cause a deadlock, panic, or freeze the CLI.",
      "status": "Not Started"
    }
  ],
  "continuousVerification": {
    "buildCommands": [
      "make build",
      "make test",
      "make test-short",
      "make lint",
      "make fmt"
    ],
    "integrationTargets": [
      "make integration-test-termmux",
      "make integration-test-prsplit-mcp",
      "make test-prsplit-fast",
      "make test-prsplit-all"
    ],
    "platformTargets": [
      "make make-all-with-log",
      "make make-all-in-container",
      "make make-all-run-windows"
    ],
    "coverageRequirement": "100% unit test coverage for new InteractiveSession interface and all session-switching logic. 100% coverage for pr-split TUI logic. Integration tests must exercise end-to-end workflows on all supported platforms."
  },
  "finalEnforcementProtocol": [
    "Every task marked Done must pass two contiguous Rule of Two reviews (subagent verification) before the mark is final.",
    "All project-defined checks must pass on all project-defined platforms before a task is considered complete.",
    "Acceptance criteria are the source of truth for 'Done', not just code existence or tests passing.",
    "Do not accept 'good enough' — the goal is exemplary, human-quality code with zero apparent AI slop.",
    "If stuck or uncertain, re-read DIRECTIVE.txt and this blueprint's Definition of Done section."
  ]
}