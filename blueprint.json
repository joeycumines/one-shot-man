{
  "meta": {
    "fileName": "blueprint.json",
    "project": "osm pr-split — fix spawn/toggle/exit failures + expand robustness",
    "scope": "Fix the osm pr-split command that exits immediately when using -claude-command=ollama. Harden the spawn-attach-toggle-detach lifecycle. Expand integration tests. Iterate indefinitely toward TRUE PERFECTION.",
    "maintenanceInstruction": "KEEP THE PLAN UP TO DATE AS YOU EVOLVE THE CODE. DEVIATIONS TO THE PLAN MUST BE LOGGED WITHIN THE PLAN. THE PLAN MUST BE REASSESSED HOLISTICALLY AFTER ANY CHANGE OF ANY SIZE.",
    "completionPolicy": "YOU ARE NOT DONE UNTIL YOU HAVE COMPLETED 100% OF THE BLUEPRINT. DO NOT STOP. YOU MUST REFINE AT REGULAR INTERVALS TO KEEP THE BLUEPRINT FULL. NEVER SPECIFY ANY ESTIMATE OF EFFORT OR COST. NEVER SPECIFY A PRIORITY OF ANY KIND."
  },
  "globalAlerts": [
    {"warning": "ALL build/test commands must be run via GNU Make. NEVER run go test ad-hoc without -race flag."},
    {"warning": "NEVER add entries to .deadcodeignore. If deadcode fails, wire up the code or delete it."},
    {"warning": "Tests MUST NOT mutate host state. Use t.TempDir(), t.Setenv()."},
    {"warning": "Cross-platform: ALL code must work on linux, macos, windows."},
    {"warning": "github.com/mattn/go-runewidth is BANNED. Use github.com/rivo/uniseg ONLY."}
  ],
  "mandatoryDirectives": {
    "contextManagement": "Tasks are flat and sequential. Execute in order. On session resume, read blueprint.json and WIP.md, find the first non-Done task, and continue."
  },
  "statusSection": {
    "currentState": "Fixes T001-T007 committed as 10f7d91. Rule of Two passed (T008-T009 Done). Now expanding: adding unit tests for VTerm.String(), ChildExitOutput(), Detach context, and attachChild.",
    "goalState": "osm pr-split works correctly with -claude-command=ollama (and all other providers). The spawn-switch-send-classify pipeline is robust: dead child detected before passthrough entry, diagnostic output surfaced, re-attach is safe (no goroutine leaks), the VTerm captures error output. Integration tests cover the full spawn-to-exit lifecycle. Zero goroutine leaks. Zero EAGAIN errors. Zero data loss on re-attach.",
    "replanLog": [
      "2026-02-28: Previous session blueprint (T001-T131, termmux rewrite) was 100% Done. This new blueprint covers the pr-split spawn/toggle failures discovered in production.",
      "2026-02-28: Added T006 (ReadLoop context cancellation) after discovering goroutine leak during re-attach of same Claude handle."
    ]
  },
  "sequentialTasks": [
    {
      "id": "T001",
      "task": "Fix explicit provider type flag handling in spawn",
      "description": "In pr_split_script.js spawn(), the explicit type path was unconditionally prepending --dangerously-skip-permissions. Changed to detect provider type from command basename: only prepend if basename contains claude.",
      "acceptance": "When -claude-command=ollama is used, --dangerously-skip-permissions is NOT prepended. When -claude-command=claude is used, it IS prepended.",
      "status": "Done"
    },
    {
      "id": "T002",
      "task": "Add post-spawn health check in ClaudeCodeExecutor",
      "description": "After registry.spawn() returns a handle, wait 0.3s then check handle.isAlive(). If dead, capture output via handle.receive() and return diagnostic error.",
      "acceptance": "A process that exits within 0.3s of spawn is detected with descriptive error including process output.",
      "status": "Done"
    },
    {
      "id": "T003",
      "task": "Add isAlive() guards before attach/switchToClaude",
      "description": "The claude command handler and auto-split attach path now check isAlive() before proceeding. If dead, surface diagnostics.",
      "acceptance": "No panic or hang when entering passthrough with a dead child. User sees Claude process has exited message.",
      "status": "Done"
    },
    {
      "id": "T004",
      "task": "Surface child exit output in switchToClaude response",
      "description": "switchToClaude binding calls tuiMux.ChildExitOutput() on ExitChildExit, includes childOutput in response. Requires Mux.ChildExitOutput() and VTerm.String().",
      "acceptance": "When Claude exits during passthrough, user sees process last terminal output. VTerm.String() returns clean plain text.",
      "status": "Done"
    },
    {
      "id": "T005",
      "task": "Handle re-attach gracefully via attachChild helper",
      "description": "attachChild helper catches ErrAlreadyAttached, detaches first, retries. Detach binding tolerates ErrDetached.",
      "acceptance": "Attach after Attach does not panic. Detach when nothing attached does not panic.",
      "status": "Done"
    },
    {
      "id": "T006",
      "task": "Fix goroutine leak on Detach: cancel ReadLoop context",
      "description": "Attach() creates cancellable context for ReadLoop. Detach() calls cancel before waiting for teeDone.",
      "acceptance": "After Detach, ReadLoop context is cancelled. No goroutine accumulation on re-attach cycles. All tests pass with -race.",
      "status": "Done"
    },
    {
      "id": "T007",
      "task": "Delete unused VTerm.CursorPos() (deadcode)",
      "description": "CursorPos() had no consumer. Deleted per AGENTS.md deadcode policy.",
      "acceptance": "make lint passes. CursorPos() no longer exists.",
      "status": "Done"
    },
    {
      "id": "T008",
      "task": "Rule of Two verification (Pass 1: lint + targeted tests)",
      "description": "Subagent verifies: make build, make lint, make integration-test-termmux, make test-pr-split-pty.",
      "acceptance": "All four make targets pass with exit code 0.",
      "status": "Done"
    },
    {
      "id": "T009",
      "task": "Rule of Two verification (Pass 2: full test suite)",
      "description": "Second subagent runs make test. Confirms zero failures across all packages.",
      "acceptance": "make test passes with exit code 0. All packages ok.",
      "status": "Done"
    },
    {
      "id": "T010",
      "task": "Commit the fixes as a coherent changeset",
      "description": "Committed as 10f7d91. 5 files, 282 insertions, 938 deletions.",
      "acceptance": "git log shows new commit on wip branch. git diff HEAD is empty.",
      "status": "Done"
    },
    {
      "id": "T011",
      "task": "Add unit test for VTerm.String() plain-text rendering",
      "description": "In vt_test.go: empty VTerm, ASCII text, wide chars, ANSI stripped, trailing spaces stripped, trailing empty lines trimmed.",
      "acceptance": "TestVTerm_String passes with -race. All 6 scenarios covered.",
      "status": "Done"
    },
    {
      "id": "T012",
      "task": "Add test for Mux.ChildExitOutput() lifecycle",
      "description": "In termmux_test.go: TestChildExitOutput_EmptyBeforeAttach, TestChildExitOutput_CapturesChildOutput, TestChildExitOutput_EmptyAfterDetach. All 3 scenarios covered.",
      "acceptance": "TestChildExitOutput passes with -race.",
      "status": "Done"
    },
    {
      "id": "T013",
      "task": "Add test for Detach ReadLoop context cancellation",
      "description": "In termmux_test.go: TestDetach_CancelsReaderContext verifies context cancel takes effect when child produces data. TestDetach_ReattachSingleReadLoop verifies re-attach captures only new child output.",
      "acceptance": "TestDetach_CancelsReaderContext and TestDetach_ReattachSingleReadLoop pass with -race.",
      "status": "Done"
    },
    {
      "id": "T014",
      "task": "Add test for attachChild ErrAlreadyAttached retry",
      "description": "In termmux_test.go: TestAttach_AfterDetach_Succeeds verifies the Attach→ErrAlreadyAttached→Detach→Attach pattern works correctly.",
      "acceptance": "Test passes. Second child connected without panic.",
      "status": "Done"
    },
    {
      "id": "T015",
      "task": "Update WIP.md with current session state",
      "description": "Rewrite WIP.md reflecting fixes done, expansion tasks ahead, file paths, branch.",
      "acceptance": "Cold-start agent can resume immediately from WIP.md.",
      "status": "In Progress"
    },
    {
      "id": "T016",
      "task": "Run full make pipeline after all tests added",
      "description": "Execute make or make-all-with-log. Verify zero failures across all packages.",
      "acceptance": "Exit code 0. All packages pass.",
      "status": "Not Started"
    }
  ],
  "continuousVerification": {
    "mandate": "Every change must pass: make build, make lint, make test.",
    "frequency": "After every task completion, before marking Done."
  },
  "finalEnforcementProtocol": {
    "reminder": "You are NOT done until blueprint.json is 100%. Scope must expand. Feed both wolves."
  }
}
