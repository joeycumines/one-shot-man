'use strict';
// pr_split_13_tui.js — TUI mode, buildCommands, mode registration
// Dependencies: all prior chunks (00-12) must be loaded first
// Requires Go-injected globals: tui, ctx, output, shared

(function(prSplit) {
    // TODO: Extract from monolith (lines 4904-6225)
})(globalThis.prSplit);
