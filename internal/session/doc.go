// Package session provides automatic session ID determination
// and resolution. Session IDs are derived from the terminal
// session environment, enabling state persistence across
// command invocations within the same terminal.
package session
